/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/rnd/*
 * Consolidates 22 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as experiments from '@/lib/api-handlers/rnd/experiments';
import * as innovations from '@/lib/api-handlers/rnd/innovations';
import * as metrics from '@/lib/api-handlers/rnd/metrics';
import * as patents from '@/lib/api-handlers/rnd/patents';
import * as projects from '@/lib/api-handlers/rnd/projects';
import * as stats from '@/lib/api-handlers/rnd/stats';
import * as experiments___id__complete from '@/lib/api-handlers/rnd/experiments___id__complete';
import * as experiments___id__start from '@/lib/api-handlers/rnd/experiments___id__start';
import * as innovations___id__approve from '@/lib/api-handlers/rnd/innovations___id__approve';
import * as innovations___id__develop from '@/lib/api-handlers/rnd/innovations___id__develop';
import * as innovations___id__evaluate from '@/lib/api-handlers/rnd/innovations___id__evaluate';
import * as innovations___id__launch from '@/lib/api-handlers/rnd/innovations___id__launch';
import * as patents___id__file from '@/lib/api-handlers/rnd/patents___id__file';
import * as patents___id__grant from '@/lib/api-handlers/rnd/patents___id__grant';
import * as projects___id__approve from '@/lib/api-handlers/rnd/projects___id__approve';
import * as projects___id__complete from '@/lib/api-handlers/rnd/projects___id__complete';
import * as projects___id__pause from '@/lib/api-handlers/rnd/projects___id__pause';
import * as projects___id__start from '@/lib/api-handlers/rnd/projects___id__start';
import * as experiments___id from '@/lib/api-handlers/rnd/experiments___id';
import * as innovations___id from '@/lib/api-handlers/rnd/innovations___id';
import * as patents___id from '@/lib/api-handlers/rnd/patents___id';
import * as projects___id from '@/lib/api-handlers/rnd/projects___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["experiments"], paramNames: [], handlers: { GET: experiments.GET, POST: experiments.POST } },
  { segments: ["innovations"], paramNames: [], handlers: { GET: innovations.GET, POST: innovations.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["patents"], paramNames: [], handlers: { GET: patents.GET, POST: patents.POST } },
  { segments: ["projects"], paramNames: [], handlers: { GET: projects.GET, POST: projects.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["experiments","[id]","complete"], paramNames: ["id"], handlers: { POST: experiments___id__complete.POST } },
  { segments: ["experiments","[id]","start"], paramNames: ["id"], handlers: { POST: experiments___id__start.POST } },
  { segments: ["innovations","[id]","approve"], paramNames: ["id"], handlers: { POST: innovations___id__approve.POST } },
  { segments: ["innovations","[id]","develop"], paramNames: ["id"], handlers: { POST: innovations___id__develop.POST } },
  { segments: ["innovations","[id]","evaluate"], paramNames: ["id"], handlers: { POST: innovations___id__evaluate.POST } },
  { segments: ["innovations","[id]","launch"], paramNames: ["id"], handlers: { POST: innovations___id__launch.POST } },
  { segments: ["patents","[id]","file"], paramNames: ["id"], handlers: { POST: patents___id__file.POST } },
  { segments: ["patents","[id]","grant"], paramNames: ["id"], handlers: { POST: patents___id__grant.POST } },
  { segments: ["projects","[id]","approve"], paramNames: ["id"], handlers: { POST: projects___id__approve.POST } },
  { segments: ["projects","[id]","complete"], paramNames: ["id"], handlers: { POST: projects___id__complete.POST } },
  { segments: ["projects","[id]","pause"], paramNames: ["id"], handlers: { POST: projects___id__pause.POST } },
  { segments: ["projects","[id]","start"], paramNames: ["id"], handlers: { POST: projects___id__start.POST } },
  { segments: ["experiments","[id]"], paramNames: ["id"], handlers: { GET: experiments___id.GET, PATCH: experiments___id.PATCH, DELETE: experiments___id.DELETE } },
  { segments: ["innovations","[id]"], paramNames: ["id"], handlers: { GET: innovations___id.GET, PATCH: innovations___id.PATCH, DELETE: innovations___id.DELETE } },
  { segments: ["patents","[id]"], paramNames: ["id"], handlers: { GET: patents___id.GET, PATCH: patents___id.PATCH, DELETE: patents___id.DELETE } },
  { segments: ["projects","[id]"], paramNames: ["id"], handlers: { GET: projects___id.GET, PATCH: projects___id.PATCH, DELETE: projects___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
