/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/innovation/*
 * Consolidates 15 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as challenges from '@/lib/api-handlers/innovation/challenges';
import * as ideas from '@/lib/api-handlers/innovation/ideas';
import * as innovation_metrics from '@/lib/api-handlers/innovation/innovation_metrics';
import * as metrics from '@/lib/api-handlers/innovation/metrics';
import * as patents from '@/lib/api-handlers/innovation/patents';
import * as projects from '@/lib/api-handlers/innovation/projects';
import * as stats from '@/lib/api-handlers/innovation/stats';
import * as challenges___id__winner from '@/lib/api-handlers/innovation/challenges___id__winner';
import * as ideas___id__advance from '@/lib/api-handlers/innovation/ideas___id__advance';
import * as ideas___id__vote from '@/lib/api-handlers/innovation/ideas___id__vote';
import * as challenges___id from '@/lib/api-handlers/innovation/challenges___id';
import * as ideas___id from '@/lib/api-handlers/innovation/ideas___id';
import * as metrics___id from '@/lib/api-handlers/innovation/metrics___id';
import * as patents___id from '@/lib/api-handlers/innovation/patents___id';
import * as projects___id from '@/lib/api-handlers/innovation/projects___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["challenges"], paramNames: [], handlers: { GET: challenges.GET, POST: challenges.POST } },
  { segments: ["ideas"], paramNames: [], handlers: { GET: ideas.GET, POST: ideas.POST } },
  { segments: ["innovation-metrics"], paramNames: [], handlers: { GET: innovation_metrics.GET } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET, POST: metrics.POST } },
  { segments: ["patents"], paramNames: [], handlers: { GET: patents.GET, POST: patents.POST } },
  { segments: ["projects"], paramNames: [], handlers: { GET: projects.GET, POST: projects.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["challenges","[id]","winner"], paramNames: ["id"], handlers: { POST: challenges___id__winner.POST } },
  { segments: ["ideas","[id]","advance"], paramNames: ["id"], handlers: { POST: ideas___id__advance.POST } },
  { segments: ["ideas","[id]","vote"], paramNames: ["id"], handlers: { POST: ideas___id__vote.POST } },
  { segments: ["challenges","[id]"], paramNames: ["id"], handlers: { GET: challenges___id.GET, PATCH: challenges___id.PATCH } },
  { segments: ["ideas","[id]"], paramNames: ["id"], handlers: { GET: ideas___id.GET, PATCH: ideas___id.PATCH } },
  { segments: ["metrics","[id]"], paramNames: ["id"], handlers: { GET: metrics___id.GET, PATCH: metrics___id.PATCH, DELETE: metrics___id.DELETE } },
  { segments: ["patents","[id]"], paramNames: ["id"], handlers: { GET: patents___id.GET, PATCH: patents___id.PATCH, DELETE: patents___id.DELETE } },
  { segments: ["projects","[id]"], paramNames: ["id"], handlers: { GET: projects___id.GET, PATCH: projects___id.PATCH, DELETE: projects___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
