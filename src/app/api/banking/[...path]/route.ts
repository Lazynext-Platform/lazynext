/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/banking/*
 * Consolidates 15 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as accounts from '@/lib/api-handlers/banking/accounts';
import * as metrics from '@/lib/api-handlers/banking/metrics';
import * as reconciliations from '@/lib/api-handlers/banking/reconciliations';
import * as stats from '@/lib/api-handlers/banking/stats';
import * as transactions from '@/lib/api-handlers/banking/transactions';
import * as wires from '@/lib/api-handlers/banking/wires';
import * as accounts___id__close from '@/lib/api-handlers/banking/accounts___id__close';
import * as reconciliations___id__complete from '@/lib/api-handlers/banking/reconciliations___id__complete';
import * as transactions___id__post from '@/lib/api-handlers/banking/transactions___id__post';
import * as wires___id__approve from '@/lib/api-handlers/banking/wires___id__approve';
import * as wires___id__cancel from '@/lib/api-handlers/banking/wires___id__cancel';
import * as accounts___id from '@/lib/api-handlers/banking/accounts___id';
import * as reconciliations___id from '@/lib/api-handlers/banking/reconciliations___id';
import * as transactions___id from '@/lib/api-handlers/banking/transactions___id';
import * as wires___id from '@/lib/api-handlers/banking/wires___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["accounts"], paramNames: [], handlers: { GET: accounts.GET, POST: accounts.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["reconciliations"], paramNames: [], handlers: { GET: reconciliations.GET, POST: reconciliations.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["transactions"], paramNames: [], handlers: { GET: transactions.GET, POST: transactions.POST } },
  { segments: ["wires"], paramNames: [], handlers: { GET: wires.GET, POST: wires.POST } },
  { segments: ["accounts","[id]","close"], paramNames: ["id"], handlers: { POST: accounts___id__close.POST } },
  { segments: ["reconciliations","[id]","complete"], paramNames: ["id"], handlers: { POST: reconciliations___id__complete.POST } },
  { segments: ["transactions","[id]","post"], paramNames: ["id"], handlers: { POST: transactions___id__post.POST } },
  { segments: ["wires","[id]","approve"], paramNames: ["id"], handlers: { POST: wires___id__approve.POST } },
  { segments: ["wires","[id]","cancel"], paramNames: ["id"], handlers: { POST: wires___id__cancel.POST } },
  { segments: ["accounts","[id]"], paramNames: ["id"], handlers: { GET: accounts___id.GET, PATCH: accounts___id.PATCH, DELETE: accounts___id.DELETE } },
  { segments: ["reconciliations","[id]"], paramNames: ["id"], handlers: { GET: reconciliations___id.GET, PATCH: reconciliations___id.PATCH } },
  { segments: ["transactions","[id]"], paramNames: ["id"], handlers: { GET: transactions___id.GET, PATCH: transactions___id.PATCH, DELETE: transactions___id.DELETE } },
  { segments: ["wires","[id]"], paramNames: ["id"], handlers: { GET: wires___id.GET, PATCH: wires___id.PATCH } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
