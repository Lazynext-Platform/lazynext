/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/data-governance/*
 * Consolidates 16 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as catalog from '@/lib/api-handlers/data-governance/catalog';
import * as lineage from '@/lib/api-handlers/data-governance/lineage';
import * as mdm from '@/lib/api-handlers/data-governance/mdm';
import * as metrics from '@/lib/api-handlers/data-governance/metrics';
import * as quality_rules from '@/lib/api-handlers/data-governance/quality_rules';
import * as stats from '@/lib/api-handlers/data-governance/stats';
import * as stewardship from '@/lib/api-handlers/data-governance/stewardship';
import * as lineage__graph___entryId from '@/lib/api-handlers/data-governance/lineage__graph___entryId';
import * as mdm___id__merge from '@/lib/api-handlers/data-governance/mdm___id__merge';
import * as mdm___id__verify from '@/lib/api-handlers/data-governance/mdm___id__verify';
import * as quality_rules___id__run from '@/lib/api-handlers/data-governance/quality_rules___id__run';
import * as catalog___id from '@/lib/api-handlers/data-governance/catalog___id';
import * as lineage___id from '@/lib/api-handlers/data-governance/lineage___id';
import * as mdm___id from '@/lib/api-handlers/data-governance/mdm___id';
import * as quality_rules___id from '@/lib/api-handlers/data-governance/quality_rules___id';
import * as stewardship___id from '@/lib/api-handlers/data-governance/stewardship___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["catalog"], paramNames: [], handlers: { GET: catalog.GET, POST: catalog.POST } },
  { segments: ["lineage"], paramNames: [], handlers: { GET: lineage.GET, POST: lineage.POST } },
  { segments: ["mdm"], paramNames: [], handlers: { GET: mdm.GET, POST: mdm.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["quality-rules"], paramNames: [], handlers: { GET: quality_rules.GET, POST: quality_rules.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["stewardship"], paramNames: [], handlers: { GET: stewardship.GET, POST: stewardship.POST } },
  { segments: ["lineage","graph","[entryId]"], paramNames: ["entryId"], handlers: { GET: lineage__graph___entryId.GET } },
  { segments: ["mdm","[id]","merge"], paramNames: ["id"], handlers: { POST: mdm___id__merge.POST } },
  { segments: ["mdm","[id]","verify"], paramNames: ["id"], handlers: { POST: mdm___id__verify.POST } },
  { segments: ["quality-rules","[id]","run"], paramNames: ["id"], handlers: { POST: quality_rules___id__run.POST } },
  { segments: ["catalog","[id]"], paramNames: ["id"], handlers: { GET: catalog___id.GET, PATCH: catalog___id.PATCH, DELETE: catalog___id.DELETE } },
  { segments: ["lineage","[id]"], paramNames: ["id"], handlers: { GET: lineage___id.GET, PATCH: lineage___id.PATCH, DELETE: lineage___id.DELETE } },
  { segments: ["mdm","[id]"], paramNames: ["id"], handlers: { GET: mdm___id.GET, PATCH: mdm___id.PATCH, DELETE: mdm___id.DELETE } },
  { segments: ["quality-rules","[id]"], paramNames: ["id"], handlers: { GET: quality_rules___id.GET, PATCH: quality_rules___id.PATCH, DELETE: quality_rules___id.DELETE } },
  { segments: ["stewardship","[id]"], paramNames: ["id"], handlers: { GET: stewardship___id.GET, PATCH: stewardship___id.PATCH, DELETE: stewardship___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
