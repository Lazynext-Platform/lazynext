/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/finance/*
 * Consolidates 26 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as expenses__summary from '@/lib/api-handlers/finance/expenses__summary';
import * as payroll__summary from '@/lib/api-handlers/finance/payroll__summary';
import * as revenue__summary from '@/lib/api-handlers/finance/revenue__summary';
import * as tax__summary from '@/lib/api-handlers/finance/tax__summary';
import * as expenses from '@/lib/api-handlers/finance/expenses';
import * as invoices from '@/lib/api-handlers/finance/invoices';
import * as payroll from '@/lib/api-handlers/finance/payroll';
import * as revenue from '@/lib/api-handlers/finance/revenue';
import * as summary from '@/lib/api-handlers/finance/summary';
import * as tax from '@/lib/api-handlers/finance/tax';
import * as transactions from '@/lib/api-handlers/finance/transactions';
import * as expenses___id__approve from '@/lib/api-handlers/finance/expenses___id__approve';
import * as expenses___id__reimburse from '@/lib/api-handlers/finance/expenses___id__reimburse';
import * as expenses___id__reject from '@/lib/api-handlers/finance/expenses___id__reject';
import * as invoices___id__cancel from '@/lib/api-handlers/finance/invoices___id__cancel';
import * as invoices___id__payment from '@/lib/api-handlers/finance/invoices___id__payment';
import * as invoices___id__send from '@/lib/api-handlers/finance/invoices___id__send';
import * as payroll___id__approve from '@/lib/api-handlers/finance/payroll___id__approve';
import * as payroll___id__pay from '@/lib/api-handlers/finance/payroll___id__pay';
import * as tax___id__file from '@/lib/api-handlers/finance/tax___id__file';
import * as tax___id__pay from '@/lib/api-handlers/finance/tax___id__pay';
import * as expenses___id from '@/lib/api-handlers/finance/expenses___id';
import * as invoices___id from '@/lib/api-handlers/finance/invoices___id';
import * as payroll___id from '@/lib/api-handlers/finance/payroll___id';
import * as revenue___id from '@/lib/api-handlers/finance/revenue___id';
import * as tax___id from '@/lib/api-handlers/finance/tax___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["expenses","summary"], paramNames: [], handlers: { GET: expenses__summary.GET } },
  { segments: ["payroll","summary"], paramNames: [], handlers: { GET: payroll__summary.GET } },
  { segments: ["revenue","summary"], paramNames: [], handlers: { GET: revenue__summary.GET } },
  { segments: ["tax","summary"], paramNames: [], handlers: { GET: tax__summary.GET } },
  { segments: ["expenses"], paramNames: [], handlers: { GET: expenses.GET, POST: expenses.POST } },
  { segments: ["invoices"], paramNames: [], handlers: { GET: invoices.GET, POST: invoices.POST } },
  { segments: ["payroll"], paramNames: [], handlers: { GET: payroll.GET, POST: payroll.POST } },
  { segments: ["revenue"], paramNames: [], handlers: { GET: revenue.GET, POST: revenue.POST } },
  { segments: ["summary"], paramNames: [], handlers: { GET: summary.GET } },
  { segments: ["tax"], paramNames: [], handlers: { GET: tax.GET, POST: tax.POST } },
  { segments: ["transactions"], paramNames: [], handlers: { GET: transactions.GET, POST: transactions.POST } },
  { segments: ["expenses","[id]","approve"], paramNames: ["id"], handlers: { POST: expenses___id__approve.POST } },
  { segments: ["expenses","[id]","reimburse"], paramNames: ["id"], handlers: { POST: expenses___id__reimburse.POST } },
  { segments: ["expenses","[id]","reject"], paramNames: ["id"], handlers: { POST: expenses___id__reject.POST } },
  { segments: ["invoices","[id]","cancel"], paramNames: ["id"], handlers: { POST: invoices___id__cancel.POST } },
  { segments: ["invoices","[id]","payment"], paramNames: ["id"], handlers: { POST: invoices___id__payment.POST } },
  { segments: ["invoices","[id]","send"], paramNames: ["id"], handlers: { POST: invoices___id__send.POST } },
  { segments: ["payroll","[id]","approve"], paramNames: ["id"], handlers: { POST: payroll___id__approve.POST } },
  { segments: ["payroll","[id]","pay"], paramNames: ["id"], handlers: { POST: payroll___id__pay.POST } },
  { segments: ["tax","[id]","file"], paramNames: ["id"], handlers: { POST: tax___id__file.POST } },
  { segments: ["tax","[id]","pay"], paramNames: ["id"], handlers: { POST: tax___id__pay.POST } },
  { segments: ["expenses","[id]"], paramNames: ["id"], handlers: { GET: expenses___id.GET, PATCH: expenses___id.PATCH } },
  { segments: ["invoices","[id]"], paramNames: ["id"], handlers: { GET: invoices___id.GET, PATCH: invoices___id.PATCH, DELETE: invoices___id.DELETE } },
  { segments: ["payroll","[id]"], paramNames: ["id"], handlers: { GET: payroll___id.GET, PATCH: payroll___id.PATCH } },
  { segments: ["revenue","[id]"], paramNames: ["id"], handlers: { PATCH: revenue___id.PATCH } },
  { segments: ["tax","[id]"], paramNames: ["id"], handlers: { GET: tax___id.GET, PATCH: tax___id.PATCH } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
