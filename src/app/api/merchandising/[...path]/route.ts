/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/merchandising/*
 * Consolidates 18 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as assortments from '@/lib/api-handlers/merchandising/assortments';
import * as metrics from '@/lib/api-handlers/merchandising/metrics';
import * as planograms from '@/lib/api-handlers/merchandising/planograms';
import * as pricing from '@/lib/api-handlers/merchandising/pricing';
import * as promotions from '@/lib/api-handlers/merchandising/promotions';
import * as stats from '@/lib/api-handlers/merchandising/stats';
import * as assortments___id__activate from '@/lib/api-handlers/merchandising/assortments___id__activate';
import * as assortments___id__discontinue from '@/lib/api-handlers/merchandising/assortments___id__discontinue';
import * as planograms___id__archive from '@/lib/api-handlers/merchandising/planograms___id__archive';
import * as planograms___id__publish from '@/lib/api-handlers/merchandising/planograms___id__publish';
import * as pricing___id__expire from '@/lib/api-handlers/merchandising/pricing___id__expire';
import * as promotions___id__activate from '@/lib/api-handlers/merchandising/promotions___id__activate';
import * as promotions___id__complete from '@/lib/api-handlers/merchandising/promotions___id__complete';
import * as promotions___id__schedule from '@/lib/api-handlers/merchandising/promotions___id__schedule';
import * as assortments___id from '@/lib/api-handlers/merchandising/assortments___id';
import * as planograms___id from '@/lib/api-handlers/merchandising/planograms___id';
import * as pricing___id from '@/lib/api-handlers/merchandising/pricing___id';
import * as promotions___id from '@/lib/api-handlers/merchandising/promotions___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["assortments"], paramNames: [], handlers: { GET: assortments.GET, POST: assortments.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["planograms"], paramNames: [], handlers: { GET: planograms.GET, POST: planograms.POST } },
  { segments: ["pricing"], paramNames: [], handlers: { GET: pricing.GET, POST: pricing.POST } },
  { segments: ["promotions"], paramNames: [], handlers: { GET: promotions.GET, POST: promotions.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["assortments","[id]","activate"], paramNames: ["id"], handlers: { POST: assortments___id__activate.POST } },
  { segments: ["assortments","[id]","discontinue"], paramNames: ["id"], handlers: { POST: assortments___id__discontinue.POST } },
  { segments: ["planograms","[id]","archive"], paramNames: ["id"], handlers: { POST: planograms___id__archive.POST } },
  { segments: ["planograms","[id]","publish"], paramNames: ["id"], handlers: { POST: planograms___id__publish.POST } },
  { segments: ["pricing","[id]","expire"], paramNames: ["id"], handlers: { POST: pricing___id__expire.POST } },
  { segments: ["promotions","[id]","activate"], paramNames: ["id"], handlers: { POST: promotions___id__activate.POST } },
  { segments: ["promotions","[id]","complete"], paramNames: ["id"], handlers: { POST: promotions___id__complete.POST } },
  { segments: ["promotions","[id]","schedule"], paramNames: ["id"], handlers: { POST: promotions___id__schedule.POST } },
  { segments: ["assortments","[id]"], paramNames: ["id"], handlers: { GET: assortments___id.GET, PATCH: assortments___id.PATCH, DELETE: assortments___id.DELETE } },
  { segments: ["planograms","[id]"], paramNames: ["id"], handlers: { GET: planograms___id.GET, PATCH: planograms___id.PATCH, DELETE: planograms___id.DELETE } },
  { segments: ["pricing","[id]"], paramNames: ["id"], handlers: { GET: pricing___id.GET, PATCH: pricing___id.PATCH, DELETE: pricing___id.DELETE } },
  { segments: ["promotions","[id]"], paramNames: ["id"], handlers: { GET: promotions___id.GET, PATCH: promotions___id.PATCH, DELETE: promotions___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
