/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/compliance-audit/*
 * Consolidates 17 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as audit_reports from '@/lib/api-handlers/compliance-audit/audit_reports';
import * as compliance_score from '@/lib/api-handlers/compliance-audit/compliance_score';
import * as control_tests from '@/lib/api-handlers/compliance-audit/control_tests';
import * as controls from '@/lib/api-handlers/compliance-audit/controls';
import * as evidence from '@/lib/api-handlers/compliance-audit/evidence';
import * as findings from '@/lib/api-handlers/compliance-audit/findings';
import * as frameworks from '@/lib/api-handlers/compliance-audit/frameworks';
import * as gap_analysis from '@/lib/api-handlers/compliance-audit/gap_analysis';
import * as stats from '@/lib/api-handlers/compliance-audit/stats';
import * as audit_reports___id__finalize from '@/lib/api-handlers/compliance-audit/audit_reports___id__finalize';
import * as findings___id__remediate from '@/lib/api-handlers/compliance-audit/findings___id__remediate';
import * as audit_reports___id from '@/lib/api-handlers/compliance-audit/audit_reports___id';
import * as control_tests___id from '@/lib/api-handlers/compliance-audit/control_tests___id';
import * as controls___id from '@/lib/api-handlers/compliance-audit/controls___id';
import * as evidence___id from '@/lib/api-handlers/compliance-audit/evidence___id';
import * as findings___id from '@/lib/api-handlers/compliance-audit/findings___id';
import * as frameworks___id from '@/lib/api-handlers/compliance-audit/frameworks___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["audit-reports"], paramNames: [], handlers: { GET: audit_reports.GET, POST: audit_reports.POST } },
  { segments: ["compliance-score"], paramNames: [], handlers: { GET: compliance_score.GET } },
  { segments: ["control-tests"], paramNames: [], handlers: { GET: control_tests.GET, POST: control_tests.POST } },
  { segments: ["controls"], paramNames: [], handlers: { GET: controls.GET, POST: controls.POST } },
  { segments: ["evidence"], paramNames: [], handlers: { GET: evidence.GET, POST: evidence.POST } },
  { segments: ["findings"], paramNames: [], handlers: { GET: findings.GET, POST: findings.POST } },
  { segments: ["frameworks"], paramNames: [], handlers: { GET: frameworks.GET, POST: frameworks.POST } },
  { segments: ["gap-analysis"], paramNames: [], handlers: { GET: gap_analysis.GET } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["audit-reports","[id]","finalize"], paramNames: ["id"], handlers: { POST: audit_reports___id__finalize.POST } },
  { segments: ["findings","[id]","remediate"], paramNames: ["id"], handlers: { POST: findings___id__remediate.POST } },
  { segments: ["audit-reports","[id]"], paramNames: ["id"], handlers: { GET: audit_reports___id.GET, PATCH: audit_reports___id.PATCH } },
  { segments: ["control-tests","[id]"], paramNames: ["id"], handlers: { GET: control_tests___id.GET, PATCH: control_tests___id.PATCH } },
  { segments: ["controls","[id]"], paramNames: ["id"], handlers: { GET: controls___id.GET, PATCH: controls___id.PATCH, DELETE: controls___id.DELETE } },
  { segments: ["evidence","[id]"], paramNames: ["id"], handlers: { GET: evidence___id.GET, DELETE: evidence___id.DELETE } },
  { segments: ["findings","[id]"], paramNames: ["id"], handlers: { GET: findings___id.GET, PATCH: findings___id.PATCH } },
  { segments: ["frameworks","[id]"], paramNames: ["id"], handlers: { GET: frameworks___id.GET, PATCH: frameworks___id.PATCH, DELETE: frameworks___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
