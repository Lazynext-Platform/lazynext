/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/team/*
 * Consolidates 16 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as channels__direct from '@/lib/api-handlers/team/channels__direct';
import * as messages__recent from '@/lib/api-handlers/team/messages__recent';
import * as messages__search from '@/lib/api-handlers/team/messages__search';
import * as moderation__flag from '@/lib/api-handlers/team/moderation__flag';
import * as moderation__flags from '@/lib/api-handlers/team/moderation__flags';
import * as moderation__log from '@/lib/api-handlers/team/moderation__log';
import * as moderation__mute from '@/lib/api-handlers/team/moderation__mute';
import * as channels from '@/lib/api-handlers/team/channels';
import * as messages from '@/lib/api-handlers/team/messages';
import * as stats from '@/lib/api-handlers/team/stats';
import * as moderation__flags___id__resolve from '@/lib/api-handlers/team/moderation__flags___id__resolve';
import * as channels___id__members from '@/lib/api-handlers/team/channels___id__members';
import * as channels___id__pinned from '@/lib/api-handlers/team/channels___id__pinned';
import * as messages___id__reactions from '@/lib/api-handlers/team/messages___id__reactions';
import * as channels___id from '@/lib/api-handlers/team/channels___id';
import * as messages___id from '@/lib/api-handlers/team/messages___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["channels","direct"], paramNames: [], handlers: { POST: channels__direct.POST } },
  { segments: ["messages","recent"], paramNames: [], handlers: { GET: messages__recent.GET } },
  { segments: ["messages","search"], paramNames: [], handlers: { GET: messages__search.GET } },
  { segments: ["moderation","flag"], paramNames: [], handlers: { POST: moderation__flag.POST } },
  { segments: ["moderation","flags"], paramNames: [], handlers: { GET: moderation__flags.GET } },
  { segments: ["moderation","log"], paramNames: [], handlers: { GET: moderation__log.GET } },
  { segments: ["moderation","mute"], paramNames: [], handlers: { POST: moderation__mute.POST, DELETE: moderation__mute.DELETE } },
  { segments: ["channels"], paramNames: [], handlers: { GET: channels.GET, POST: channels.POST } },
  { segments: ["messages"], paramNames: [], handlers: { GET: messages.GET, POST: messages.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["moderation","flags","[id]","resolve"], paramNames: ["id"], handlers: { POST: moderation__flags___id__resolve.POST } },
  { segments: ["channels","[id]","members"], paramNames: ["id"], handlers: { POST: channels___id__members.POST, DELETE: channels___id__members.DELETE } },
  { segments: ["channels","[id]","pinned"], paramNames: ["id"], handlers: { GET: channels___id__pinned.GET, POST: channels___id__pinned.POST, DELETE: channels___id__pinned.DELETE } },
  { segments: ["messages","[id]","reactions"], paramNames: ["id"], handlers: { POST: messages___id__reactions.POST, DELETE: messages___id__reactions.DELETE } },
  { segments: ["channels","[id]"], paramNames: ["id"], handlers: { GET: channels___id.GET, PATCH: channels___id.PATCH, DELETE: channels___id.DELETE } },
  { segments: ["messages","[id]"], paramNames: ["id"], handlers: { GET: messages___id.GET, PATCH: messages___id.PATCH, DELETE: messages___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
