/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/employee-development/*
 * Consolidates 21 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as certifications__expiring from '@/lib/api-handlers/employee-development/certifications__expiring';
import * as certifications__stats from '@/lib/api-handlers/employee-development/certifications__stats';
import * as onboarding__stats from '@/lib/api-handlers/employee-development/onboarding__stats';
import * as onboarding__tasks from '@/lib/api-handlers/employee-development/onboarding__tasks';
import * as onboarding__templates from '@/lib/api-handlers/employee-development/onboarding__templates';
import * as skills__matrix from '@/lib/api-handlers/employee-development/skills__matrix';
import * as training__stats from '@/lib/api-handlers/employee-development/training__stats';
import * as certifications from '@/lib/api-handlers/employee-development/certifications';
import * as skills from '@/lib/api-handlers/employee-development/skills';
import * as stats from '@/lib/api-handlers/employee-development/stats';
import * as training from '@/lib/api-handlers/employee-development/training';
import * as onboarding__tasks___id__complete from '@/lib/api-handlers/employee-development/onboarding__tasks___id__complete';
import * as onboarding__tasks___id__skip from '@/lib/api-handlers/employee-development/onboarding__tasks___id__skip';
import * as onboarding__templates___id__apply from '@/lib/api-handlers/employee-development/onboarding__templates___id__apply';
import * as onboarding__progress___employeeId from '@/lib/api-handlers/employee-development/onboarding__progress___employeeId';
import * as onboarding__tasks___id from '@/lib/api-handlers/employee-development/onboarding__tasks___id';
import * as training___id__complete from '@/lib/api-handlers/employee-development/training___id__complete';
import * as training___id__progress from '@/lib/api-handlers/employee-development/training___id__progress';
import * as career_path___employeeId from '@/lib/api-handlers/employee-development/career_path___employeeId';
import * as certifications___id from '@/lib/api-handlers/employee-development/certifications___id';
import * as training___id from '@/lib/api-handlers/employee-development/training___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["certifications","expiring"], paramNames: [], handlers: { GET: certifications__expiring.GET } },
  { segments: ["certifications","stats"], paramNames: [], handlers: { GET: certifications__stats.GET } },
  { segments: ["onboarding","stats"], paramNames: [], handlers: { GET: onboarding__stats.GET } },
  { segments: ["onboarding","tasks"], paramNames: [], handlers: { GET: onboarding__tasks.GET, POST: onboarding__tasks.POST } },
  { segments: ["onboarding","templates"], paramNames: [], handlers: { GET: onboarding__templates.GET, POST: onboarding__templates.POST } },
  { segments: ["skills","matrix"], paramNames: [], handlers: { GET: skills__matrix.GET } },
  { segments: ["training","stats"], paramNames: [], handlers: { GET: training__stats.GET } },
  { segments: ["certifications"], paramNames: [], handlers: { GET: certifications.GET, POST: certifications.POST } },
  { segments: ["skills"], paramNames: [], handlers: { POST: skills.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["training"], paramNames: [], handlers: { GET: training.GET, POST: training.POST } },
  { segments: ["onboarding","tasks","[id]","complete"], paramNames: ["id"], handlers: { POST: onboarding__tasks___id__complete.POST } },
  { segments: ["onboarding","tasks","[id]","skip"], paramNames: ["id"], handlers: { POST: onboarding__tasks___id__skip.POST } },
  { segments: ["onboarding","templates","[id]","apply"], paramNames: ["id"], handlers: { POST: onboarding__templates___id__apply.POST } },
  { segments: ["onboarding","progress","[employeeId]"], paramNames: ["employeeId"], handlers: { GET: onboarding__progress___employeeId.GET } },
  { segments: ["onboarding","tasks","[id]"], paramNames: ["id"], handlers: { GET: onboarding__tasks___id.GET, PATCH: onboarding__tasks___id.PATCH, DELETE: onboarding__tasks___id.DELETE } },
  { segments: ["training","[id]","complete"], paramNames: ["id"], handlers: { POST: training___id__complete.POST } },
  { segments: ["training","[id]","progress"], paramNames: ["id"], handlers: { POST: training___id__progress.POST } },
  { segments: ["career-path","[employeeId]"], paramNames: ["employeeId"], handlers: { GET: career_path___employeeId.GET, POST: career_path___employeeId.POST } },
  { segments: ["certifications","[id]"], paramNames: ["id"], handlers: { GET: certifications___id.GET, PATCH: certifications___id.PATCH, DELETE: certifications___id.DELETE } },
  { segments: ["training","[id]"], paramNames: ["id"], handlers: { GET: training___id.GET, PATCH: training___id.PATCH, DELETE: training___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
