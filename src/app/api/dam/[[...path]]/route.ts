/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/dam/*
 * Consolidates 12 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as assets from '@/lib/api-handlers/dam/assets';
import * as collections from '@/lib/api-handlers/dam/collections';
import * as metrics from '@/lib/api-handlers/dam/metrics';
import * as permissions from '@/lib/api-handlers/dam/permissions';
import * as stats from '@/lib/api-handlers/dam/stats';
import * as versions from '@/lib/api-handlers/dam/versions';
import * as assets___id__archive from '@/lib/api-handlers/dam/assets___id__archive';
import * as assets___id__restrict from '@/lib/api-handlers/dam/assets___id__restrict';
import * as assets___id from '@/lib/api-handlers/dam/assets___id';
import * as collections___id from '@/lib/api-handlers/dam/collections___id';
import * as permissions___id from '@/lib/api-handlers/dam/permissions___id';
import * as versions___id from '@/lib/api-handlers/dam/versions___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["assets"], paramNames: [], handlers: { GET: assets.GET, POST: assets.POST } },
  { segments: ["collections"], paramNames: [], handlers: { GET: collections.GET, POST: collections.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["permissions"], paramNames: [], handlers: { GET: permissions.GET, POST: permissions.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["versions"], paramNames: [], handlers: { GET: versions.GET, POST: versions.POST } },
  { segments: ["assets","[id]","archive"], paramNames: ["id"], handlers: { POST: assets___id__archive.POST } },
  { segments: ["assets","[id]","restrict"], paramNames: ["id"], handlers: { POST: assets___id__restrict.POST } },
  { segments: ["assets","[id]"], paramNames: ["id"], handlers: { GET: assets___id.GET, PATCH: assets___id.PATCH, DELETE: assets___id.DELETE } },
  { segments: ["collections","[id]"], paramNames: ["id"], handlers: { GET: collections___id.GET, PATCH: collections___id.PATCH, DELETE: collections___id.DELETE } },
  { segments: ["permissions","[id]"], paramNames: ["id"], handlers: { GET: permissions___id.GET, PATCH: permissions___id.PATCH, DELETE: permissions___id.DELETE } },
  { segments: ["versions","[id]"], paramNames: ["id"], handlers: { GET: versions___id.GET, PATCH: versions___id.PATCH, DELETE: versions___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path?: string[] }> },
) {
  const { path = [] } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
