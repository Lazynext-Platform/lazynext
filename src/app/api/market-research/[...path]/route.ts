/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/market-research/*
 * Consolidates 16 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as competitors from '@/lib/api-handlers/market-research/competitors';
import * as insights from '@/lib/api-handlers/market-research/insights';
import * as metrics from '@/lib/api-handlers/market-research/metrics';
import * as projects from '@/lib/api-handlers/market-research/projects';
import * as segments from '@/lib/api-handlers/market-research/segments';
import * as stats from '@/lib/api-handlers/market-research/stats';
import * as insights___id__action from '@/lib/api-handlers/market-research/insights___id__action';
import * as insights___id__validate from '@/lib/api-handlers/market-research/insights___id__validate';
import * as projects___id__complete from '@/lib/api-handlers/market-research/projects___id__complete';
import * as projects___id__hold from '@/lib/api-handlers/market-research/projects___id__hold';
import * as projects___id__start from '@/lib/api-handlers/market-research/projects___id__start';
import * as segments___id__archive from '@/lib/api-handlers/market-research/segments___id__archive';
import * as competitors___id from '@/lib/api-handlers/market-research/competitors___id';
import * as insights___id from '@/lib/api-handlers/market-research/insights___id';
import * as projects___id from '@/lib/api-handlers/market-research/projects___id';
import * as segments___id from '@/lib/api-handlers/market-research/segments___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["competitors"], paramNames: [], handlers: { GET: competitors.GET, POST: competitors.POST } },
  { segments: ["insights"], paramNames: [], handlers: { GET: insights.GET, POST: insights.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["projects"], paramNames: [], handlers: { GET: projects.GET, POST: projects.POST } },
  { segments: ["segments"], paramNames: [], handlers: { GET: segments.GET, POST: segments.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["insights","[id]","action"], paramNames: ["id"], handlers: { POST: insights___id__action.POST } },
  { segments: ["insights","[id]","validate"], paramNames: ["id"], handlers: { POST: insights___id__validate.POST } },
  { segments: ["projects","[id]","complete"], paramNames: ["id"], handlers: { POST: projects___id__complete.POST } },
  { segments: ["projects","[id]","hold"], paramNames: ["id"], handlers: { POST: projects___id__hold.POST } },
  { segments: ["projects","[id]","start"], paramNames: ["id"], handlers: { POST: projects___id__start.POST } },
  { segments: ["segments","[id]","archive"], paramNames: ["id"], handlers: { POST: segments___id__archive.POST } },
  { segments: ["competitors","[id]"], paramNames: ["id"], handlers: { GET: competitors___id.GET, PATCH: competitors___id.PATCH, DELETE: competitors___id.DELETE } },
  { segments: ["insights","[id]"], paramNames: ["id"], handlers: { GET: insights___id.GET, PATCH: insights___id.PATCH, DELETE: insights___id.DELETE } },
  { segments: ["projects","[id]"], paramNames: ["id"], handlers: { GET: projects___id.GET, PATCH: projects___id.PATCH, DELETE: projects___id.DELETE } },
  { segments: ["segments","[id]"], paramNames: ["id"], handlers: { GET: segments___id.GET, PATCH: segments___id.PATCH, DELETE: segments___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
