/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/corporate-library/*
 * Consolidates 19 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as acquisitions from '@/lib/api-handlers/corporate-library/acquisitions';
import * as items from '@/lib/api-handlers/corporate-library/items';
import * as loans from '@/lib/api-handlers/corporate-library/loans';
import * as metrics from '@/lib/api-handlers/corporate-library/metrics';
import * as reservations from '@/lib/api-handlers/corporate-library/reservations';
import * as stats from '@/lib/api-handlers/corporate-library/stats';
import * as acquisitions___id__approve from '@/lib/api-handlers/corporate-library/acquisitions___id__approve';
import * as acquisitions___id__order from '@/lib/api-handlers/corporate-library/acquisitions___id__order';
import * as acquisitions___id__receive from '@/lib/api-handlers/corporate-library/acquisitions___id__receive';
import * as acquisitions___id__reject from '@/lib/api-handlers/corporate-library/acquisitions___id__reject';
import * as loans___id__lost from '@/lib/api-handlers/corporate-library/loans___id__lost';
import * as loans___id__renew from '@/lib/api-handlers/corporate-library/loans___id__renew';
import * as loans___id__return from '@/lib/api-handlers/corporate-library/loans___id__return';
import * as reservations___id__cancel from '@/lib/api-handlers/corporate-library/reservations___id__cancel';
import * as reservations___id__fulfill from '@/lib/api-handlers/corporate-library/reservations___id__fulfill';
import * as acquisitions___id from '@/lib/api-handlers/corporate-library/acquisitions___id';
import * as items___id from '@/lib/api-handlers/corporate-library/items___id';
import * as loans___id from '@/lib/api-handlers/corporate-library/loans___id';
import * as reservations___id from '@/lib/api-handlers/corporate-library/reservations___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["acquisitions"], paramNames: [], handlers: { GET: acquisitions.GET, POST: acquisitions.POST } },
  { segments: ["items"], paramNames: [], handlers: { GET: items.GET, POST: items.POST } },
  { segments: ["loans"], paramNames: [], handlers: { GET: loans.GET, POST: loans.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["reservations"], paramNames: [], handlers: { GET: reservations.GET, POST: reservations.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["acquisitions","[id]","approve"], paramNames: ["id"], handlers: { POST: acquisitions___id__approve.POST } },
  { segments: ["acquisitions","[id]","order"], paramNames: ["id"], handlers: { POST: acquisitions___id__order.POST } },
  { segments: ["acquisitions","[id]","receive"], paramNames: ["id"], handlers: { POST: acquisitions___id__receive.POST } },
  { segments: ["acquisitions","[id]","reject"], paramNames: ["id"], handlers: { POST: acquisitions___id__reject.POST } },
  { segments: ["loans","[id]","lost"], paramNames: ["id"], handlers: { POST: loans___id__lost.POST } },
  { segments: ["loans","[id]","renew"], paramNames: ["id"], handlers: { POST: loans___id__renew.POST } },
  { segments: ["loans","[id]","return"], paramNames: ["id"], handlers: { POST: loans___id__return.POST } },
  { segments: ["reservations","[id]","cancel"], paramNames: ["id"], handlers: { POST: reservations___id__cancel.POST } },
  { segments: ["reservations","[id]","fulfill"], paramNames: ["id"], handlers: { POST: reservations___id__fulfill.POST } },
  { segments: ["acquisitions","[id]"], paramNames: ["id"], handlers: { GET: acquisitions___id.GET, PATCH: acquisitions___id.PATCH, DELETE: acquisitions___id.DELETE } },
  { segments: ["items","[id]"], paramNames: ["id"], handlers: { GET: items___id.GET, PATCH: items___id.PATCH, DELETE: items___id.DELETE } },
  { segments: ["loans","[id]"], paramNames: ["id"], handlers: { GET: loans___id.GET, PATCH: loans___id.PATCH, DELETE: loans___id.DELETE } },
  { segments: ["reservations","[id]"], paramNames: ["id"], handlers: { GET: reservations___id.GET, PATCH: reservations___id.PATCH, DELETE: reservations___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path?: string[] }> },
) {
  const { path = [] } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
