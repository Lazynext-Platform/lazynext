/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/employee-surveys/*
 * Consolidates 17 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as campaigns from '@/lib/api-handlers/employee-surveys/campaigns';
import * as metrics from '@/lib/api-handlers/employee-surveys/metrics';
import * as questions from '@/lib/api-handlers/employee-surveys/questions';
import * as responses from '@/lib/api-handlers/employee-surveys/responses';
import * as stats from '@/lib/api-handlers/employee-surveys/stats';
import * as surveys from '@/lib/api-handlers/employee-surveys/surveys';
import * as campaigns___id__complete from '@/lib/api-handlers/employee-surveys/campaigns___id__complete';
import * as campaigns___id__start from '@/lib/api-handlers/employee-surveys/campaigns___id__start';
import * as responses___id__submit from '@/lib/api-handlers/employee-surveys/responses___id__submit';
import * as surveys___id__activate from '@/lib/api-handlers/employee-surveys/surveys___id__activate';
import * as surveys___id__close from '@/lib/api-handlers/employee-surveys/surveys___id__close';
import * as surveys___id__pause from '@/lib/api-handlers/employee-surveys/surveys___id__pause';
import * as surveys___id__publish from '@/lib/api-handlers/employee-surveys/surveys___id__publish';
import * as campaigns___id from '@/lib/api-handlers/employee-surveys/campaigns___id';
import * as questions___id from '@/lib/api-handlers/employee-surveys/questions___id';
import * as responses___id from '@/lib/api-handlers/employee-surveys/responses___id';
import * as surveys___id from '@/lib/api-handlers/employee-surveys/surveys___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["campaigns"], paramNames: [], handlers: { GET: campaigns.GET, POST: campaigns.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["questions"], paramNames: [], handlers: { GET: questions.GET, POST: questions.POST } },
  { segments: ["responses"], paramNames: [], handlers: { GET: responses.GET, POST: responses.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["surveys"], paramNames: [], handlers: { GET: surveys.GET, POST: surveys.POST } },
  { segments: ["campaigns","[id]","complete"], paramNames: ["id"], handlers: { POST: campaigns___id__complete.POST } },
  { segments: ["campaigns","[id]","start"], paramNames: ["id"], handlers: { POST: campaigns___id__start.POST } },
  { segments: ["responses","[id]","submit"], paramNames: ["id"], handlers: { POST: responses___id__submit.POST } },
  { segments: ["surveys","[id]","activate"], paramNames: ["id"], handlers: { POST: surveys___id__activate.POST } },
  { segments: ["surveys","[id]","close"], paramNames: ["id"], handlers: { POST: surveys___id__close.POST } },
  { segments: ["surveys","[id]","pause"], paramNames: ["id"], handlers: { POST: surveys___id__pause.POST } },
  { segments: ["surveys","[id]","publish"], paramNames: ["id"], handlers: { POST: surveys___id__publish.POST } },
  { segments: ["campaigns","[id]"], paramNames: ["id"], handlers: { GET: campaigns___id.GET, PATCH: campaigns___id.PATCH, DELETE: campaigns___id.DELETE } },
  { segments: ["questions","[id]"], paramNames: ["id"], handlers: { GET: questions___id.GET, PATCH: questions___id.PATCH, DELETE: questions___id.DELETE } },
  { segments: ["responses","[id]"], paramNames: ["id"], handlers: { GET: responses___id.GET, PATCH: responses___id.PATCH, DELETE: responses___id.DELETE } },
  { segments: ["surveys","[id]"], paramNames: ["id"], handlers: { GET: surveys___id.GET, PATCH: surveys___id.PATCH, DELETE: surveys___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
