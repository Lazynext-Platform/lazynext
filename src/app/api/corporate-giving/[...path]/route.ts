/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/corporate-giving/*
 * Consolidates 24 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as donations from '@/lib/api-handlers/corporate-giving/donations';
import * as grants from '@/lib/api-handlers/corporate-giving/grants';
import * as metrics from '@/lib/api-handlers/corporate-giving/metrics';
import * as programs from '@/lib/api-handlers/corporate-giving/programs';
import * as sponsorships from '@/lib/api-handlers/corporate-giving/sponsorships';
import * as stats from '@/lib/api-handlers/corporate-giving/stats';
import * as donations___id__approve from '@/lib/api-handlers/corporate-giving/donations___id__approve';
import * as donations___id__complete from '@/lib/api-handlers/corporate-giving/donations___id__complete';
import * as donations___id__reject from '@/lib/api-handlers/corporate-giving/donations___id__reject';
import * as grants___id__approve from '@/lib/api-handlers/corporate-giving/grants___id__approve';
import * as grants___id__complete from '@/lib/api-handlers/corporate-giving/grants___id__complete';
import * as grants___id__disburse from '@/lib/api-handlers/corporate-giving/grants___id__disburse';
import * as grants___id__reject from '@/lib/api-handlers/corporate-giving/grants___id__reject';
import * as grants___id__submit from '@/lib/api-handlers/corporate-giving/grants___id__submit';
import * as programs___id__complete from '@/lib/api-handlers/corporate-giving/programs___id__complete';
import * as programs___id__pause from '@/lib/api-handlers/corporate-giving/programs___id__pause';
import * as programs___id__start from '@/lib/api-handlers/corporate-giving/programs___id__start';
import * as sponsorships___id__activate from '@/lib/api-handlers/corporate-giving/sponsorships___id__activate';
import * as sponsorships___id__cancel from '@/lib/api-handlers/corporate-giving/sponsorships___id__cancel';
import * as sponsorships___id__complete from '@/lib/api-handlers/corporate-giving/sponsorships___id__complete';
import * as donations___id from '@/lib/api-handlers/corporate-giving/donations___id';
import * as grants___id from '@/lib/api-handlers/corporate-giving/grants___id';
import * as programs___id from '@/lib/api-handlers/corporate-giving/programs___id';
import * as sponsorships___id from '@/lib/api-handlers/corporate-giving/sponsorships___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["donations"], paramNames: [], handlers: { GET: donations.GET, POST: donations.POST } },
  { segments: ["grants"], paramNames: [], handlers: { GET: grants.GET, POST: grants.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["programs"], paramNames: [], handlers: { GET: programs.GET, POST: programs.POST } },
  { segments: ["sponsorships"], paramNames: [], handlers: { GET: sponsorships.GET, POST: sponsorships.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["donations","[id]","approve"], paramNames: ["id"], handlers: { POST: donations___id__approve.POST } },
  { segments: ["donations","[id]","complete"], paramNames: ["id"], handlers: { POST: donations___id__complete.POST } },
  { segments: ["donations","[id]","reject"], paramNames: ["id"], handlers: { POST: donations___id__reject.POST } },
  { segments: ["grants","[id]","approve"], paramNames: ["id"], handlers: { POST: grants___id__approve.POST } },
  { segments: ["grants","[id]","complete"], paramNames: ["id"], handlers: { POST: grants___id__complete.POST } },
  { segments: ["grants","[id]","disburse"], paramNames: ["id"], handlers: { POST: grants___id__disburse.POST } },
  { segments: ["grants","[id]","reject"], paramNames: ["id"], handlers: { POST: grants___id__reject.POST } },
  { segments: ["grants","[id]","submit"], paramNames: ["id"], handlers: { POST: grants___id__submit.POST } },
  { segments: ["programs","[id]","complete"], paramNames: ["id"], handlers: { POST: programs___id__complete.POST } },
  { segments: ["programs","[id]","pause"], paramNames: ["id"], handlers: { POST: programs___id__pause.POST } },
  { segments: ["programs","[id]","start"], paramNames: ["id"], handlers: { POST: programs___id__start.POST } },
  { segments: ["sponsorships","[id]","activate"], paramNames: ["id"], handlers: { POST: sponsorships___id__activate.POST } },
  { segments: ["sponsorships","[id]","cancel"], paramNames: ["id"], handlers: { POST: sponsorships___id__cancel.POST } },
  { segments: ["sponsorships","[id]","complete"], paramNames: ["id"], handlers: { POST: sponsorships___id__complete.POST } },
  { segments: ["donations","[id]"], paramNames: ["id"], handlers: { GET: donations___id.GET, PATCH: donations___id.PATCH, DELETE: donations___id.DELETE } },
  { segments: ["grants","[id]"], paramNames: ["id"], handlers: { GET: grants___id.GET, PATCH: grants___id.PATCH, DELETE: grants___id.DELETE } },
  { segments: ["programs","[id]"], paramNames: ["id"], handlers: { GET: programs___id.GET, PATCH: programs___id.PATCH, DELETE: programs___id.DELETE } },
  { segments: ["sponsorships","[id]"], paramNames: ["id"], handlers: { GET: sponsorships___id.GET, PATCH: sponsorships___id.PATCH, DELETE: sponsorships___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
