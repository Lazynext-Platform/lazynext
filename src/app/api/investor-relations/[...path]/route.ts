/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/investor-relations/*
 * Consolidates 15 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as cap_table__summary from '@/lib/api-handlers/investor-relations/cap_table__summary';
import * as cap_table from '@/lib/api-handlers/investor-relations/cap_table';
import * as communications from '@/lib/api-handlers/investor-relations/communications';
import * as funding_rounds from '@/lib/api-handlers/investor-relations/funding_rounds';
import * as investors from '@/lib/api-handlers/investor-relations/investors';
import * as metrics from '@/lib/api-handlers/investor-relations/metrics';
import * as stats from '@/lib/api-handlers/investor-relations/stats';
import * as updates from '@/lib/api-handlers/investor-relations/updates';
import * as funding_rounds___id__close from '@/lib/api-handlers/investor-relations/funding_rounds___id__close';
import * as updates___id__publish from '@/lib/api-handlers/investor-relations/updates___id__publish';
import * as cap_table___id from '@/lib/api-handlers/investor-relations/cap_table___id';
import * as communications___id from '@/lib/api-handlers/investor-relations/communications___id';
import * as funding_rounds___id from '@/lib/api-handlers/investor-relations/funding_rounds___id';
import * as investors___id from '@/lib/api-handlers/investor-relations/investors___id';
import * as updates___id from '@/lib/api-handlers/investor-relations/updates___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["cap-table","summary"], paramNames: [], handlers: { GET: cap_table__summary.GET } },
  { segments: ["cap-table"], paramNames: [], handlers: { GET: cap_table.GET, POST: cap_table.POST } },
  { segments: ["communications"], paramNames: [], handlers: { GET: communications.GET, POST: communications.POST } },
  { segments: ["funding-rounds"], paramNames: [], handlers: { GET: funding_rounds.GET, POST: funding_rounds.POST } },
  { segments: ["investors"], paramNames: [], handlers: { GET: investors.GET, POST: investors.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["updates"], paramNames: [], handlers: { GET: updates.GET, POST: updates.POST } },
  { segments: ["funding-rounds","[id]","close"], paramNames: ["id"], handlers: { POST: funding_rounds___id__close.POST } },
  { segments: ["updates","[id]","publish"], paramNames: ["id"], handlers: { POST: updates___id__publish.POST } },
  { segments: ["cap-table","[id]"], paramNames: ["id"], handlers: { GET: cap_table___id.GET, PATCH: cap_table___id.PATCH, DELETE: cap_table___id.DELETE } },
  { segments: ["communications","[id]"], paramNames: ["id"], handlers: { GET: communications___id.GET, PATCH: communications___id.PATCH } },
  { segments: ["funding-rounds","[id]"], paramNames: ["id"], handlers: { GET: funding_rounds___id.GET, PATCH: funding_rounds___id.PATCH } },
  { segments: ["investors","[id]"], paramNames: ["id"], handlers: { GET: investors___id.GET, PATCH: investors___id.PATCH, DELETE: investors___id.DELETE } },
  { segments: ["updates","[id]"], paramNames: ["id"], handlers: { GET: updates___id.GET, PATCH: updates___id.PATCH } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
