/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/document-management/*
 * Consolidates 18 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as retention__expired from '@/lib/api-handlers/document-management/retention__expired';
import * as classifications from '@/lib/api-handlers/document-management/classifications';
import * as documents from '@/lib/api-handlers/document-management/documents';
import * as esign from '@/lib/api-handlers/document-management/esign';
import * as retention from '@/lib/api-handlers/document-management/retention';
import * as stats from '@/lib/api-handlers/document-management/stats';
import * as templates from '@/lib/api-handlers/document-management/templates';
import * as documents___id__versions from '@/lib/api-handlers/document-management/documents___id__versions';
import * as esign___id__cancel from '@/lib/api-handlers/document-management/esign___id__cancel';
import * as esign___id__sign from '@/lib/api-handlers/document-management/esign___id__sign';
import * as esign___id__signatures from '@/lib/api-handlers/document-management/esign___id__signatures';
import * as audit_trail___documentId from '@/lib/api-handlers/document-management/audit_trail___documentId';
import * as classifications___id from '@/lib/api-handlers/document-management/classifications___id';
import * as documents___id from '@/lib/api-handlers/document-management/documents___id';
import * as esign___id from '@/lib/api-handlers/document-management/esign___id';
import * as retention___id from '@/lib/api-handlers/document-management/retention___id';
import * as templates___id from '@/lib/api-handlers/document-management/templates___id';
import * as documents___id__versions___vid from '@/lib/api-handlers/document-management/documents___id__versions___vid';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["retention","expired"], paramNames: [], handlers: { GET: retention__expired.GET } },
  { segments: ["classifications"], paramNames: [], handlers: { GET: classifications.GET, POST: classifications.POST } },
  { segments: ["documents"], paramNames: [], handlers: { GET: documents.GET, POST: documents.POST } },
  { segments: ["esign"], paramNames: [], handlers: { GET: esign.GET, POST: esign.POST } },
  { segments: ["retention"], paramNames: [], handlers: { GET: retention.GET, POST: retention.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["templates"], paramNames: [], handlers: { GET: templates.GET, POST: templates.POST } },
  { segments: ["documents","[id]","versions"], paramNames: ["id"], handlers: { GET: documents___id__versions.GET } },
  { segments: ["esign","[id]","cancel"], paramNames: ["id"], handlers: { POST: esign___id__cancel.POST } },
  { segments: ["esign","[id]","sign"], paramNames: ["id"], handlers: { POST: esign___id__sign.POST } },
  { segments: ["esign","[id]","signatures"], paramNames: ["id"], handlers: { GET: esign___id__signatures.GET } },
  { segments: ["audit-trail","[documentId]"], paramNames: ["documentId"], handlers: { GET: audit_trail___documentId.GET } },
  { segments: ["classifications","[id]"], paramNames: ["id"], handlers: { PATCH: classifications___id.PATCH, DELETE: classifications___id.DELETE } },
  { segments: ["documents","[id]"], paramNames: ["id"], handlers: { GET: documents___id.GET, PATCH: documents___id.PATCH, DELETE: documents___id.DELETE } },
  { segments: ["esign","[id]"], paramNames: ["id"], handlers: { GET: esign___id.GET } },
  { segments: ["retention","[id]"], paramNames: ["id"], handlers: { PATCH: retention___id.PATCH, DELETE: retention___id.DELETE } },
  { segments: ["templates","[id]"], paramNames: ["id"], handlers: { GET: templates___id.GET, PATCH: templates___id.PATCH, DELETE: templates___id.DELETE } },
  { segments: ["documents","[id]","versions","[vid]"], paramNames: ["id","vid"], handlers: { POST: documents___id__versions___vid.POST } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
