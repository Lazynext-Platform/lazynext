/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/travel/*
 * Consolidates 11 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as cost_summary from '@/lib/api-handlers/travel/cost_summary';
import * as requests from '@/lib/api-handlers/travel/requests';
import * as stats from '@/lib/api-handlers/travel/stats';
import * as requests___id__approve from '@/lib/api-handlers/travel/requests___id__approve';
import * as requests___id__bookings from '@/lib/api-handlers/travel/requests___id__bookings';
import * as requests___id__cancel from '@/lib/api-handlers/travel/requests___id__cancel';
import * as requests___id__convert_expense from '@/lib/api-handlers/travel/requests___id__convert_expense';
import * as requests___id__itinerary from '@/lib/api-handlers/travel/requests___id__itinerary';
import * as requests___id__policy_check from '@/lib/api-handlers/travel/requests___id__policy_check';
import * as requests___id__reject from '@/lib/api-handlers/travel/requests___id__reject';
import * as requests___id from '@/lib/api-handlers/travel/requests___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["cost-summary"], paramNames: [], handlers: { GET: cost_summary.GET } },
  { segments: ["requests"], paramNames: [], handlers: { GET: requests.GET, POST: requests.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["requests","[id]","approve"], paramNames: ["id"], handlers: { POST: requests___id__approve.POST } },
  { segments: ["requests","[id]","bookings"], paramNames: ["id"], handlers: { GET: requests___id__bookings.GET, POST: requests___id__bookings.POST } },
  { segments: ["requests","[id]","cancel"], paramNames: ["id"], handlers: { POST: requests___id__cancel.POST } },
  { segments: ["requests","[id]","convert-expense"], paramNames: ["id"], handlers: { POST: requests___id__convert_expense.POST } },
  { segments: ["requests","[id]","itinerary"], paramNames: ["id"], handlers: { GET: requests___id__itinerary.GET, POST: requests___id__itinerary.POST } },
  { segments: ["requests","[id]","policy-check"], paramNames: ["id"], handlers: { GET: requests___id__policy_check.GET } },
  { segments: ["requests","[id]","reject"], paramNames: ["id"], handlers: { POST: requests___id__reject.POST } },
  { segments: ["requests","[id]"], paramNames: ["id"], handlers: { GET: requests___id.GET, PATCH: requests___id.PATCH } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
