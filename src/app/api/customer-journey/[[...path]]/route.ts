/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/customer-journey/*
 * Consolidates 15 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as journeys from '@/lib/api-handlers/customer-journey/journeys';
import * as metrics from '@/lib/api-handlers/customer-journey/metrics';
import * as scores from '@/lib/api-handlers/customer-journey/scores';
import * as stages from '@/lib/api-handlers/customer-journey/stages';
import * as stats from '@/lib/api-handlers/customer-journey/stats';
import * as touchpoints from '@/lib/api-handlers/customer-journey/touchpoints';
import * as journeys___id__activate from '@/lib/api-handlers/customer-journey/journeys___id__activate';
import * as journeys___id__archive from '@/lib/api-handlers/customer-journey/journeys___id__archive';
import * as scores___id__action from '@/lib/api-handlers/customer-journey/scores___id__action';
import * as scores___id__analyze from '@/lib/api-handlers/customer-journey/scores___id__analyze';
import * as touchpoints___id__deactivate from '@/lib/api-handlers/customer-journey/touchpoints___id__deactivate';
import * as journeys___id from '@/lib/api-handlers/customer-journey/journeys___id';
import * as scores___id from '@/lib/api-handlers/customer-journey/scores___id';
import * as stages___id from '@/lib/api-handlers/customer-journey/stages___id';
import * as touchpoints___id from '@/lib/api-handlers/customer-journey/touchpoints___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["journeys"], paramNames: [], handlers: { GET: journeys.GET, POST: journeys.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["scores"], paramNames: [], handlers: { GET: scores.GET, POST: scores.POST } },
  { segments: ["stages"], paramNames: [], handlers: { GET: stages.GET, POST: stages.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["touchpoints"], paramNames: [], handlers: { GET: touchpoints.GET, POST: touchpoints.POST } },
  { segments: ["journeys","[id]","activate"], paramNames: ["id"], handlers: { POST: journeys___id__activate.POST } },
  { segments: ["journeys","[id]","archive"], paramNames: ["id"], handlers: { POST: journeys___id__archive.POST } },
  { segments: ["scores","[id]","action"], paramNames: ["id"], handlers: { POST: scores___id__action.POST } },
  { segments: ["scores","[id]","analyze"], paramNames: ["id"], handlers: { POST: scores___id__analyze.POST } },
  { segments: ["touchpoints","[id]","deactivate"], paramNames: ["id"], handlers: { POST: touchpoints___id__deactivate.POST } },
  { segments: ["journeys","[id]"], paramNames: ["id"], handlers: { GET: journeys___id.GET, PATCH: journeys___id.PATCH, DELETE: journeys___id.DELETE } },
  { segments: ["scores","[id]"], paramNames: ["id"], handlers: { GET: scores___id.GET, PATCH: scores___id.PATCH, DELETE: scores___id.DELETE } },
  { segments: ["stages","[id]"], paramNames: ["id"], handlers: { GET: stages___id.GET, PATCH: stages___id.PATCH, DELETE: stages___id.DELETE } },
  { segments: ["touchpoints","[id]"], paramNames: ["id"], handlers: { GET: touchpoints___id.GET, PATCH: touchpoints___id.PATCH, DELETE: touchpoints___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path?: string[] }> },
) {
  const { path = [] } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
