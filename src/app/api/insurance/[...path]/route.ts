/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/insurance/*
 * Consolidates 15 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as brokers from '@/lib/api-handlers/insurance/brokers';
import * as claims from '@/lib/api-handlers/insurance/claims';
import * as coverages from '@/lib/api-handlers/insurance/coverages';
import * as expiring from '@/lib/api-handlers/insurance/expiring';
import * as metrics from '@/lib/api-handlers/insurance/metrics';
import * as policies from '@/lib/api-handlers/insurance/policies';
import * as stats from '@/lib/api-handlers/insurance/stats';
import * as claims___id__approve from '@/lib/api-handlers/insurance/claims___id__approve';
import * as claims___id__deny from '@/lib/api-handlers/insurance/claims___id__deny';
import * as claims___id__settle from '@/lib/api-handlers/insurance/claims___id__settle';
import * as policies___id__renew from '@/lib/api-handlers/insurance/policies___id__renew';
import * as brokers___id from '@/lib/api-handlers/insurance/brokers___id';
import * as claims___id from '@/lib/api-handlers/insurance/claims___id';
import * as coverages___id from '@/lib/api-handlers/insurance/coverages___id';
import * as policies___id from '@/lib/api-handlers/insurance/policies___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routes: RouteEntry[] = [
  { segments: ["brokers"], paramNames: [], handlers: { GET: brokers.GET, POST: brokers.POST } },
  { segments: ["claims"], paramNames: [], handlers: { GET: claims.GET, POST: claims.POST } },
  { segments: ["coverages"], paramNames: [], handlers: { GET: coverages.GET, POST: coverages.POST } },
  { segments: ["expiring"], paramNames: [], handlers: { GET: expiring.GET } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["policies"], paramNames: [], handlers: { GET: policies.GET, POST: policies.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["claims","[id]","approve"], paramNames: ["id"], handlers: { POST: claims___id__approve.POST } },
  { segments: ["claims","[id]","deny"], paramNames: ["id"], handlers: { POST: claims___id__deny.POST } },
  { segments: ["claims","[id]","settle"], paramNames: ["id"], handlers: { POST: claims___id__settle.POST } },
  { segments: ["policies","[id]","renew"], paramNames: ["id"], handlers: { POST: policies___id__renew.POST } },
  { segments: ["brokers","[id]"], paramNames: ["id"], handlers: { GET: brokers___id.GET, PATCH: brokers___id.PATCH, DELETE: brokers___id.DELETE } },
  { segments: ["claims","[id]"], paramNames: ["id"], handlers: { GET: claims___id.GET, PATCH: claims___id.PATCH } },
  { segments: ["coverages","[id]"], paramNames: ["id"], handlers: { GET: coverages___id.GET, PATCH: coverages___id.PATCH, DELETE: coverages___id.DELETE } },
  { segments: ["policies","[id]"], paramNames: ["id"], handlers: { GET: policies___id.GET, PATCH: policies___id.PATCH, DELETE: policies___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
