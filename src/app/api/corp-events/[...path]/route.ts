/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/corp-events/*
 * Consolidates 17 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as events from '@/lib/api-handlers/corp-events/events';
import * as metrics from '@/lib/api-handlers/corp-events/metrics';
import * as registrations from '@/lib/api-handlers/corp-events/registrations';
import * as speakers from '@/lib/api-handlers/corp-events/speakers';
import * as stats from '@/lib/api-handlers/corp-events/stats';
import * as venues from '@/lib/api-handlers/corp-events/venues';
import * as events___id__complete from '@/lib/api-handlers/corp-events/events___id__complete';
import * as events___id__open_registration from '@/lib/api-handlers/corp-events/events___id__open_registration';
import * as registrations___id__cancel from '@/lib/api-handlers/corp-events/registrations___id__cancel';
import * as registrations___id__confirm from '@/lib/api-handlers/corp-events/registrations___id__confirm';
import * as speakers___id__confirm from '@/lib/api-handlers/corp-events/speakers___id__confirm';
import * as speakers___id__decline from '@/lib/api-handlers/corp-events/speakers___id__decline';
import * as venues___id__book from '@/lib/api-handlers/corp-events/venues___id__book';
import * as events___id from '@/lib/api-handlers/corp-events/events___id';
import * as registrations___id from '@/lib/api-handlers/corp-events/registrations___id';
import * as speakers___id from '@/lib/api-handlers/corp-events/speakers___id';
import * as venues___id from '@/lib/api-handlers/corp-events/venues___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["events"], paramNames: [], handlers: { GET: events.GET, POST: events.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["registrations"], paramNames: [], handlers: { GET: registrations.GET, POST: registrations.POST } },
  { segments: ["speakers"], paramNames: [], handlers: { GET: speakers.GET, POST: speakers.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["venues"], paramNames: [], handlers: { GET: venues.GET, POST: venues.POST } },
  { segments: ["events","[id]","complete"], paramNames: ["id"], handlers: { POST: events___id__complete.POST } },
  { segments: ["events","[id]","open-registration"], paramNames: ["id"], handlers: { POST: events___id__open_registration.POST } },
  { segments: ["registrations","[id]","cancel"], paramNames: ["id"], handlers: { POST: registrations___id__cancel.POST } },
  { segments: ["registrations","[id]","confirm"], paramNames: ["id"], handlers: { POST: registrations___id__confirm.POST } },
  { segments: ["speakers","[id]","confirm"], paramNames: ["id"], handlers: { POST: speakers___id__confirm.POST } },
  { segments: ["speakers","[id]","decline"], paramNames: ["id"], handlers: { POST: speakers___id__decline.POST } },
  { segments: ["venues","[id]","book"], paramNames: ["id"], handlers: { POST: venues___id__book.POST } },
  { segments: ["events","[id]"], paramNames: ["id"], handlers: { GET: events___id.GET, PATCH: events___id.PATCH, DELETE: events___id.DELETE } },
  { segments: ["registrations","[id]"], paramNames: ["id"], handlers: { GET: registrations___id.GET, PATCH: registrations___id.PATCH } },
  { segments: ["speakers","[id]"], paramNames: ["id"], handlers: { GET: speakers___id.GET, PATCH: speakers___id.PATCH } },
  { segments: ["venues","[id]"], paramNames: ["id"], handlers: { GET: venues___id.GET, PATCH: venues___id.PATCH, DELETE: venues___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
