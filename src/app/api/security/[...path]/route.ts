/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/security/*
 * Consolidates 28 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as security_keys__auth__verify from '@/lib/api-handlers/security/security_keys__auth__verify';
import * as security_keys__register__verify from '@/lib/api-handlers/security/security_keys__register__verify';
import * as _2fa__backup_codes from '@/lib/api-handlers/security/2fa__backup_codes';
import * as _2fa__disable from '@/lib/api-handlers/security/2fa__disable';
import * as _2fa__setup from '@/lib/api-handlers/security/2fa__setup';
import * as _2fa__status from '@/lib/api-handlers/security/2fa__status';
import * as _2fa__verify from '@/lib/api-handlers/security/2fa__verify';
import * as password__history from '@/lib/api-handlers/security/password__history';
import * as password__policy from '@/lib/api-handlers/security/password__policy';
import * as password__validate from '@/lib/api-handlers/security/password__validate';
import * as security_keys__auth from '@/lib/api-handlers/security/security_keys__auth';
import * as security_keys__register from '@/lib/api-handlers/security/security_keys__register';
import * as sessions__revoke_all from '@/lib/api-handlers/security/sessions__revoke_all';
import * as sso__config from '@/lib/api-handlers/security/sso__config';
import * as sso__metadata from '@/lib/api-handlers/security/sso__metadata';
import * as events from '@/lib/api-handlers/security/events';
import * as login_history from '@/lib/api-handlers/security/login_history';
import * as migrate_tokens from '@/lib/api-handlers/security/migrate_tokens';
import * as sandbox_config from '@/lib/api-handlers/security/sandbox_config';
import * as security_keys from '@/lib/api-handlers/security/security_keys';
import * as sessions from '@/lib/api-handlers/security/sessions';
import * as summary from '@/lib/api-handlers/security/summary';
import * as suspicious from '@/lib/api-handlers/security/suspicious';
import * as tenant_audit from '@/lib/api-handlers/security/tenant_audit';
import * as sso__config___id from '@/lib/api-handlers/security/sso__config___id';
import * as events___id from '@/lib/api-handlers/security/events___id';
import * as security_keys___id from '@/lib/api-handlers/security/security_keys___id';
import * as sessions___id from '@/lib/api-handlers/security/sessions___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routes: RouteEntry[] = [
  { segments: ["security-keys","auth","verify"], paramNames: [], handlers: { POST: security_keys__auth__verify.POST } },
  { segments: ["security-keys","register","verify"], paramNames: [], handlers: { POST: security_keys__register__verify.POST } },
  { segments: ["2fa","backup-codes"], paramNames: [], handlers: { GET: _2fa__backup_codes.GET, POST: _2fa__backup_codes.POST } },
  { segments: ["2fa","disable"], paramNames: [], handlers: { POST: _2fa__disable.POST } },
  { segments: ["2fa","setup"], paramNames: [], handlers: { POST: _2fa__setup.POST } },
  { segments: ["2fa","status"], paramNames: [], handlers: { GET: _2fa__status.GET } },
  { segments: ["2fa","verify"], paramNames: [], handlers: { POST: _2fa__verify.POST } },
  { segments: ["password","history"], paramNames: [], handlers: { POST: password__history.POST } },
  { segments: ["password","policy"], paramNames: [], handlers: { GET: password__policy.GET, PATCH: password__policy.PATCH } },
  { segments: ["password","validate"], paramNames: [], handlers: { POST: password__validate.POST } },
  { segments: ["security-keys","auth"], paramNames: [], handlers: { POST: security_keys__auth.POST } },
  { segments: ["security-keys","register"], paramNames: [], handlers: { POST: security_keys__register.POST } },
  { segments: ["sessions","revoke-all"], paramNames: [], handlers: { POST: sessions__revoke_all.POST } },
  { segments: ["sso","config"], paramNames: [], handlers: { GET: sso__config.GET, POST: sso__config.POST } },
  { segments: ["sso","metadata"], paramNames: [], handlers: { GET: sso__metadata.GET } },
  { segments: ["events"], paramNames: [], handlers: { GET: events.GET, POST: events.POST } },
  { segments: ["login-history"], paramNames: [], handlers: { GET: login_history.GET } },
  { segments: ["migrate-tokens"], paramNames: [], handlers: { POST: migrate_tokens.POST } },
  { segments: ["sandbox-config"], paramNames: [], handlers: { GET: sandbox_config.GET } },
  { segments: ["security-keys"], paramNames: [], handlers: { GET: security_keys.GET } },
  { segments: ["sessions"], paramNames: [], handlers: { GET: sessions.GET } },
  { segments: ["summary"], paramNames: [], handlers: { GET: summary.GET } },
  { segments: ["suspicious"], paramNames: [], handlers: { GET: suspicious.GET } },
  { segments: ["tenant-audit"], paramNames: [], handlers: { GET: tenant_audit.GET } },
  { segments: ["sso","config","[id]"], paramNames: ["id"], handlers: { DELETE: sso__config___id.DELETE } },
  { segments: ["events","[id]"], paramNames: ["id"], handlers: { GET: events___id.GET, PATCH: events___id.PATCH } },
  { segments: ["security-keys","[id]"], paramNames: ["id"], handlers: { PATCH: security_keys___id.PATCH, DELETE: security_keys___id.DELETE } },
  { segments: ["sessions","[id]"], paramNames: ["id"], handlers: { DELETE: sessions___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
