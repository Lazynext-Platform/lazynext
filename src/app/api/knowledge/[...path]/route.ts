/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/knowledge/*
 * Consolidates 18 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as search__reindex from '@/lib/api-handlers/knowledge/search__reindex';
import * as bases from '@/lib/api-handlers/knowledge/bases';
import * as documents from '@/lib/api-handlers/knowledge/documents';
import * as search from '@/lib/api-handlers/knowledge/search';
import * as stats from '@/lib/api-handlers/knowledge/stats';
import * as tags from '@/lib/api-handlers/knowledge/tags';
import * as tree from '@/lib/api-handlers/knowledge/tree';
import * as bases___id__articles from '@/lib/api-handlers/knowledge/bases___id__articles';
import * as documents___id__backlinks from '@/lib/api-handlers/knowledge/documents___id__backlinks';
import * as documents___id__children from '@/lib/api-handlers/knowledge/documents___id__children';
import * as documents___id__links from '@/lib/api-handlers/knowledge/documents___id__links';
import * as documents___id__move from '@/lib/api-handlers/knowledge/documents___id__move';
import * as documents___id__versions from '@/lib/api-handlers/knowledge/documents___id__versions';
import * as bases___id from '@/lib/api-handlers/knowledge/bases___id';
import * as documents___id from '@/lib/api-handlers/knowledge/documents___id';
import * as links___id from '@/lib/api-handlers/knowledge/links___id';
import * as bases___id__articles___articleId from '@/lib/api-handlers/knowledge/bases___id__articles___articleId';
import * as documents___id__versions___version from '@/lib/api-handlers/knowledge/documents___id__versions___version';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["search","reindex"], paramNames: [], handlers: { POST: search__reindex.POST } },
  { segments: ["bases"], paramNames: [], handlers: { GET: bases.GET, POST: bases.POST } },
  { segments: ["documents"], paramNames: [], handlers: { GET: documents.GET, POST: documents.POST } },
  { segments: ["search"], paramNames: [], handlers: { GET: search.GET } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["tags"], paramNames: [], handlers: { GET: tags.GET } },
  { segments: ["tree"], paramNames: [], handlers: { GET: tree.GET } },
  { segments: ["bases","[id]","articles"], paramNames: ["id"], handlers: { GET: bases___id__articles.GET, POST: bases___id__articles.POST } },
  { segments: ["documents","[id]","backlinks"], paramNames: ["id"], handlers: { GET: documents___id__backlinks.GET } },
  { segments: ["documents","[id]","children"], paramNames: ["id"], handlers: { GET: documents___id__children.GET } },
  { segments: ["documents","[id]","links"], paramNames: ["id"], handlers: { GET: documents___id__links.GET, POST: documents___id__links.POST } },
  { segments: ["documents","[id]","move"], paramNames: ["id"], handlers: { POST: documents___id__move.POST } },
  { segments: ["documents","[id]","versions"], paramNames: ["id"], handlers: { GET: documents___id__versions.GET } },
  { segments: ["bases","[id]"], paramNames: ["id"], handlers: { GET: bases___id.GET, PATCH: bases___id.PATCH, DELETE: bases___id.DELETE } },
  { segments: ["documents","[id]"], paramNames: ["id"], handlers: { GET: documents___id.GET, PATCH: documents___id.PATCH, DELETE: documents___id.DELETE } },
  { segments: ["links","[id]"], paramNames: ["id"], handlers: { DELETE: links___id.DELETE } },
  { segments: ["bases","[id]","articles","[articleId]"], paramNames: ["id","articleId"], handlers: { GET: bases___id__articles___articleId.GET, PATCH: bases___id__articles___articleId.PATCH, DELETE: bases___id__articles___articleId.DELETE } },
  { segments: ["documents","[id]","versions","[version]"], paramNames: ["id","version"], handlers: { GET: documents___id__versions___version.GET, POST: documents___id__versions___version.POST } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
