/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/quality-management/*
 * Consolidates 19 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as audits from '@/lib/api-handlers/quality-management/audits';
import * as capas from '@/lib/api-handlers/quality-management/capas';
import * as inspections from '@/lib/api-handlers/quality-management/inspections';
import * as metrics from '@/lib/api-handlers/quality-management/metrics';
import * as nonconformances from '@/lib/api-handlers/quality-management/nonconformances';
import * as root_cause from '@/lib/api-handlers/quality-management/root_cause';
import * as standards from '@/lib/api-handlers/quality-management/standards';
import * as stats from '@/lib/api-handlers/quality-management/stats';
import * as audits___id__complete from '@/lib/api-handlers/quality-management/audits___id__complete';
import * as capas___id__complete from '@/lib/api-handlers/quality-management/capas___id__complete';
import * as capas___id__verify from '@/lib/api-handlers/quality-management/capas___id__verify';
import * as inspections___id__complete from '@/lib/api-handlers/quality-management/inspections___id__complete';
import * as nonconformances___id__close from '@/lib/api-handlers/quality-management/nonconformances___id__close';
import * as audits___id from '@/lib/api-handlers/quality-management/audits___id';
import * as capas___id from '@/lib/api-handlers/quality-management/capas___id';
import * as inspections___id from '@/lib/api-handlers/quality-management/inspections___id';
import * as nonconformances___id from '@/lib/api-handlers/quality-management/nonconformances___id';
import * as root_cause___id from '@/lib/api-handlers/quality-management/root_cause___id';
import * as standards___id from '@/lib/api-handlers/quality-management/standards___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routes: RouteEntry[] = [
  { segments: ["audits"], paramNames: [], handlers: { GET: audits.GET, POST: audits.POST } },
  { segments: ["capas"], paramNames: [], handlers: { GET: capas.GET, POST: capas.POST } },
  { segments: ["inspections"], paramNames: [], handlers: { GET: inspections.GET, POST: inspections.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["nonconformances"], paramNames: [], handlers: { GET: nonconformances.GET, POST: nonconformances.POST } },
  { segments: ["root-cause"], paramNames: [], handlers: { GET: root_cause.GET, POST: root_cause.POST } },
  { segments: ["standards"], paramNames: [], handlers: { GET: standards.GET, POST: standards.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["audits","[id]","complete"], paramNames: ["id"], handlers: { POST: audits___id__complete.POST } },
  { segments: ["capas","[id]","complete"], paramNames: ["id"], handlers: { POST: capas___id__complete.POST } },
  { segments: ["capas","[id]","verify"], paramNames: ["id"], handlers: { POST: capas___id__verify.POST } },
  { segments: ["inspections","[id]","complete"], paramNames: ["id"], handlers: { POST: inspections___id__complete.POST } },
  { segments: ["nonconformances","[id]","close"], paramNames: ["id"], handlers: { POST: nonconformances___id__close.POST } },
  { segments: ["audits","[id]"], paramNames: ["id"], handlers: { GET: audits___id.GET, PATCH: audits___id.PATCH } },
  { segments: ["capas","[id]"], paramNames: ["id"], handlers: { GET: capas___id.GET, PATCH: capas___id.PATCH } },
  { segments: ["inspections","[id]"], paramNames: ["id"], handlers: { GET: inspections___id.GET, PATCH: inspections___id.PATCH } },
  { segments: ["nonconformances","[id]"], paramNames: ["id"], handlers: { GET: nonconformances___id.GET, PATCH: nonconformances___id.PATCH } },
  { segments: ["root-cause","[id]"], paramNames: ["id"], handlers: { GET: root_cause___id.GET } },
  { segments: ["standards","[id]"], paramNames: ["id"], handlers: { GET: standards___id.GET, PATCH: standards___id.PATCH, DELETE: standards___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
