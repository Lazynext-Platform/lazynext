/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/communications/*
 * Consolidates 16 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as crises from '@/lib/api-handlers/communications/crises';
import * as media_contacts from '@/lib/api-handlers/communications/media_contacts';
import * as mentions from '@/lib/api-handlers/communications/mentions';
import * as metrics from '@/lib/api-handlers/communications/metrics';
import * as press_releases from '@/lib/api-handlers/communications/press_releases';
import * as speaker_opportunities from '@/lib/api-handlers/communications/speaker_opportunities';
import * as stats from '@/lib/api-handlers/communications/stats';
import * as crises___id__activate from '@/lib/api-handlers/communications/crises___id__activate';
import * as crises___id__resolve from '@/lib/api-handlers/communications/crises___id__resolve';
import * as crises___id__statement from '@/lib/api-handlers/communications/crises___id__statement';
import * as press_releases___id__publish from '@/lib/api-handlers/communications/press_releases___id__publish';
import * as crises___id from '@/lib/api-handlers/communications/crises___id';
import * as media_contacts___id from '@/lib/api-handlers/communications/media_contacts___id';
import * as mentions___id from '@/lib/api-handlers/communications/mentions___id';
import * as press_releases___id from '@/lib/api-handlers/communications/press_releases___id';
import * as speaker_opportunities___id from '@/lib/api-handlers/communications/speaker_opportunities___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["crises"], paramNames: [], handlers: { GET: crises.GET, POST: crises.POST } },
  { segments: ["media-contacts"], paramNames: [], handlers: { GET: media_contacts.GET, POST: media_contacts.POST } },
  { segments: ["mentions"], paramNames: [], handlers: { GET: mentions.GET, POST: mentions.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["press-releases"], paramNames: [], handlers: { GET: press_releases.GET, POST: press_releases.POST } },
  { segments: ["speaker-opportunities"], paramNames: [], handlers: { GET: speaker_opportunities.GET, POST: speaker_opportunities.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["crises","[id]","activate"], paramNames: ["id"], handlers: { POST: crises___id__activate.POST } },
  { segments: ["crises","[id]","resolve"], paramNames: ["id"], handlers: { POST: crises___id__resolve.POST } },
  { segments: ["crises","[id]","statement"], paramNames: ["id"], handlers: { POST: crises___id__statement.POST } },
  { segments: ["press-releases","[id]","publish"], paramNames: ["id"], handlers: { POST: press_releases___id__publish.POST } },
  { segments: ["crises","[id]"], paramNames: ["id"], handlers: { GET: crises___id.GET, PATCH: crises___id.PATCH } },
  { segments: ["media-contacts","[id]"], paramNames: ["id"], handlers: { GET: media_contacts___id.GET, PATCH: media_contacts___id.PATCH, DELETE: media_contacts___id.DELETE } },
  { segments: ["mentions","[id]"], paramNames: ["id"], handlers: { GET: mentions___id.GET, PATCH: mentions___id.PATCH, DELETE: mentions___id.DELETE } },
  { segments: ["press-releases","[id]"], paramNames: ["id"], handlers: { GET: press_releases___id.GET, PATCH: press_releases___id.PATCH, DELETE: press_releases___id.DELETE } },
  { segments: ["speaker-opportunities","[id]"], paramNames: ["id"], handlers: { GET: speaker_opportunities___id.GET, PATCH: speaker_opportunities___id.PATCH } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
