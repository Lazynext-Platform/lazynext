/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/permissions/*
 * Consolidates 24 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as checks from '@/lib/api-handlers/permissions/checks';
import * as grants from '@/lib/api-handlers/permissions/grants';
import * as metrics from '@/lib/api-handlers/permissions/metrics';
import * as policies from '@/lib/api-handlers/permissions/policies';
import * as roles from '@/lib/api-handlers/permissions/roles';
import * as stats from '@/lib/api-handlers/permissions/stats';
import * as checks___id__allow from '@/lib/api-handlers/permissions/checks___id__allow';
import * as checks___id__conditional from '@/lib/api-handlers/permissions/checks___id__conditional';
import * as checks___id__deny from '@/lib/api-handlers/permissions/checks___id__deny';
import * as checks___id__error from '@/lib/api-handlers/permissions/checks___id__error';
import * as grants___id__expire from '@/lib/api-handlers/permissions/grants___id__expire';
import * as grants___id__grant from '@/lib/api-handlers/permissions/grants___id__grant';
import * as grants___id__pending from '@/lib/api-handlers/permissions/grants___id__pending';
import * as grants___id__revoke from '@/lib/api-handlers/permissions/grants___id__revoke';
import * as policies___id__activate from '@/lib/api-handlers/permissions/policies___id__activate';
import * as policies___id__archive from '@/lib/api-handlers/permissions/policies___id__archive';
import * as policies___id__deprecate from '@/lib/api-handlers/permissions/policies___id__deprecate';
import * as roles___id__activate from '@/lib/api-handlers/permissions/roles___id__activate';
import * as roles___id__deactivate from '@/lib/api-handlers/permissions/roles___id__deactivate';
import * as roles___id__deprecate from '@/lib/api-handlers/permissions/roles___id__deprecate';
import * as checks___id from '@/lib/api-handlers/permissions/checks___id';
import * as grants___id from '@/lib/api-handlers/permissions/grants___id';
import * as policies___id from '@/lib/api-handlers/permissions/policies___id';
import * as roles___id from '@/lib/api-handlers/permissions/roles___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routes: RouteEntry[] = [
  { segments: ["checks"], paramNames: [], handlers: { GET: checks.GET, POST: checks.POST } },
  { segments: ["grants"], paramNames: [], handlers: { GET: grants.GET, POST: grants.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["policies"], paramNames: [], handlers: { GET: policies.GET, POST: policies.POST } },
  { segments: ["roles"], paramNames: [], handlers: { GET: roles.GET, POST: roles.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["checks","[id]","allow"], paramNames: ["id"], handlers: { POST: checks___id__allow.POST } },
  { segments: ["checks","[id]","conditional"], paramNames: ["id"], handlers: { POST: checks___id__conditional.POST } },
  { segments: ["checks","[id]","deny"], paramNames: ["id"], handlers: { POST: checks___id__deny.POST } },
  { segments: ["checks","[id]","error"], paramNames: ["id"], handlers: { POST: checks___id__error.POST } },
  { segments: ["grants","[id]","expire"], paramNames: ["id"], handlers: { POST: grants___id__expire.POST } },
  { segments: ["grants","[id]","grant"], paramNames: ["id"], handlers: { POST: grants___id__grant.POST } },
  { segments: ["grants","[id]","pending"], paramNames: ["id"], handlers: { POST: grants___id__pending.POST } },
  { segments: ["grants","[id]","revoke"], paramNames: ["id"], handlers: { POST: grants___id__revoke.POST } },
  { segments: ["policies","[id]","activate"], paramNames: ["id"], handlers: { POST: policies___id__activate.POST } },
  { segments: ["policies","[id]","archive"], paramNames: ["id"], handlers: { POST: policies___id__archive.POST } },
  { segments: ["policies","[id]","deprecate"], paramNames: ["id"], handlers: { POST: policies___id__deprecate.POST } },
  { segments: ["roles","[id]","activate"], paramNames: ["id"], handlers: { POST: roles___id__activate.POST } },
  { segments: ["roles","[id]","deactivate"], paramNames: ["id"], handlers: { POST: roles___id__deactivate.POST } },
  { segments: ["roles","[id]","deprecate"], paramNames: ["id"], handlers: { POST: roles___id__deprecate.POST } },
  { segments: ["checks","[id]"], paramNames: ["id"], handlers: { GET: checks___id.GET, PATCH: checks___id.PATCH, DELETE: checks___id.DELETE } },
  { segments: ["grants","[id]"], paramNames: ["id"], handlers: { GET: grants___id.GET, PATCH: grants___id.PATCH, DELETE: grants___id.DELETE } },
  { segments: ["policies","[id]"], paramNames: ["id"], handlers: { GET: policies___id.GET, PATCH: policies___id.PATCH, DELETE: policies___id.DELETE } },
  { segments: ["roles","[id]"], paramNames: ["id"], handlers: { GET: roles___id.GET, PATCH: roles___id.PATCH, DELETE: roles___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
