/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/kpis/*
 * Consolidates 24 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as dashboards from '@/lib/api-handlers/kpis/dashboards';
import * as definitions from '@/lib/api-handlers/kpis/definitions';
import * as measurements from '@/lib/api-handlers/kpis/measurements';
import * as metrics from '@/lib/api-handlers/kpis/metrics';
import * as stats from '@/lib/api-handlers/kpis/stats';
import * as targets from '@/lib/api-handlers/kpis/targets';
import * as dashboards___id__activate from '@/lib/api-handlers/kpis/dashboards___id__activate';
import * as dashboards___id__archive from '@/lib/api-handlers/kpis/dashboards___id__archive';
import * as dashboards___id__deprecate from '@/lib/api-handlers/kpis/dashboards___id__deprecate';
import * as definitions___id__activate from '@/lib/api-handlers/kpis/definitions___id__activate';
import * as definitions___id__archive from '@/lib/api-handlers/kpis/definitions___id__archive';
import * as definitions___id__deprecate from '@/lib/api-handlers/kpis/definitions___id__deprecate';
import * as measurements___id__archive from '@/lib/api-handlers/kpis/measurements___id__archive';
import * as measurements___id__dispute from '@/lib/api-handlers/kpis/measurements___id__dispute';
import * as measurements___id__record from '@/lib/api-handlers/kpis/measurements___id__record';
import * as measurements___id__verify from '@/lib/api-handlers/kpis/measurements___id__verify';
import * as targets___id__achieve from '@/lib/api-handlers/kpis/targets___id__achieve';
import * as targets___id__activate from '@/lib/api-handlers/kpis/targets___id__activate';
import * as targets___id__archive from '@/lib/api-handlers/kpis/targets___id__archive';
import * as targets___id__miss from '@/lib/api-handlers/kpis/targets___id__miss';
import * as dashboards___id from '@/lib/api-handlers/kpis/dashboards___id';
import * as definitions___id from '@/lib/api-handlers/kpis/definitions___id';
import * as measurements___id from '@/lib/api-handlers/kpis/measurements___id';
import * as targets___id from '@/lib/api-handlers/kpis/targets___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routes: RouteEntry[] = [
  { segments: ["dashboards"], paramNames: [], handlers: { GET: dashboards.GET, POST: dashboards.POST } },
  { segments: ["definitions"], paramNames: [], handlers: { GET: definitions.GET, POST: definitions.POST } },
  { segments: ["measurements"], paramNames: [], handlers: { GET: measurements.GET, POST: measurements.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["targets"], paramNames: [], handlers: { GET: targets.GET, POST: targets.POST } },
  { segments: ["dashboards","[id]","activate"], paramNames: ["id"], handlers: { POST: dashboards___id__activate.POST } },
  { segments: ["dashboards","[id]","archive"], paramNames: ["id"], handlers: { POST: dashboards___id__archive.POST } },
  { segments: ["dashboards","[id]","deprecate"], paramNames: ["id"], handlers: { POST: dashboards___id__deprecate.POST } },
  { segments: ["definitions","[id]","activate"], paramNames: ["id"], handlers: { POST: definitions___id__activate.POST } },
  { segments: ["definitions","[id]","archive"], paramNames: ["id"], handlers: { POST: definitions___id__archive.POST } },
  { segments: ["definitions","[id]","deprecate"], paramNames: ["id"], handlers: { POST: definitions___id__deprecate.POST } },
  { segments: ["measurements","[id]","archive"], paramNames: ["id"], handlers: { POST: measurements___id__archive.POST } },
  { segments: ["measurements","[id]","dispute"], paramNames: ["id"], handlers: { POST: measurements___id__dispute.POST } },
  { segments: ["measurements","[id]","record"], paramNames: ["id"], handlers: { POST: measurements___id__record.POST } },
  { segments: ["measurements","[id]","verify"], paramNames: ["id"], handlers: { POST: measurements___id__verify.POST } },
  { segments: ["targets","[id]","achieve"], paramNames: ["id"], handlers: { POST: targets___id__achieve.POST } },
  { segments: ["targets","[id]","activate"], paramNames: ["id"], handlers: { POST: targets___id__activate.POST } },
  { segments: ["targets","[id]","archive"], paramNames: ["id"], handlers: { POST: targets___id__archive.POST } },
  { segments: ["targets","[id]","miss"], paramNames: ["id"], handlers: { POST: targets___id__miss.POST } },
  { segments: ["dashboards","[id]"], paramNames: ["id"], handlers: { GET: dashboards___id.GET, PATCH: dashboards___id.PATCH, DELETE: dashboards___id.DELETE } },
  { segments: ["definitions","[id]"], paramNames: ["id"], handlers: { GET: definitions___id.GET, PATCH: definitions___id.PATCH, DELETE: definitions___id.DELETE } },
  { segments: ["measurements","[id]"], paramNames: ["id"], handlers: { GET: measurements___id.GET, PATCH: measurements___id.PATCH, DELETE: measurements___id.DELETE } },
  { segments: ["targets","[id]"], paramNames: ["id"], handlers: { GET: targets___id.GET, PATCH: targets___id.PATCH, DELETE: targets___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
