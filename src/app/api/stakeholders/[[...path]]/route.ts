/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/stakeholders/*
 * Consolidates 14 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as communications from '@/lib/api-handlers/stakeholders/communications';
import * as engagements from '@/lib/api-handlers/stakeholders/engagements';
import * as matrix from '@/lib/api-handlers/stakeholders/matrix';
import * as metrics from '@/lib/api-handlers/stakeholders/metrics';
import * as sentiments from '@/lib/api-handlers/stakeholders/sentiments';
import * as stats from '@/lib/api-handlers/stakeholders/stats';
import * as index from '@/lib/api-handlers/stakeholders/index';
import * as communications___id__response from '@/lib/api-handlers/stakeholders/communications___id__response';
import * as communications___id__send from '@/lib/api-handlers/stakeholders/communications___id__send';
import * as engagements___id__complete from '@/lib/api-handlers/stakeholders/engagements___id__complete';
import * as communications___id from '@/lib/api-handlers/stakeholders/communications___id';
import * as engagements___id from '@/lib/api-handlers/stakeholders/engagements___id';
import * as sentiments___id from '@/lib/api-handlers/stakeholders/sentiments___id';
import * as _id from '@/lib/api-handlers/stakeholders/_id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["communications"], paramNames: [], handlers: { GET: communications.GET, POST: communications.POST } },
  { segments: ["engagements"], paramNames: [], handlers: { GET: engagements.GET, POST: engagements.POST } },
  { segments: ["matrix"], paramNames: [], handlers: { GET: matrix.GET } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["sentiments"], paramNames: [], handlers: { GET: sentiments.GET, POST: sentiments.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: [], paramNames: [], handlers: { GET: index.GET, POST: index.POST } },
  { segments: ["communications","[id]","response"], paramNames: ["id"], handlers: { POST: communications___id__response.POST } },
  { segments: ["communications","[id]","send"], paramNames: ["id"], handlers: { POST: communications___id__send.POST } },
  { segments: ["engagements","[id]","complete"], paramNames: ["id"], handlers: { POST: engagements___id__complete.POST } },
  { segments: ["communications","[id]"], paramNames: ["id"], handlers: { GET: communications___id.GET, PATCH: communications___id.PATCH } },
  { segments: ["engagements","[id]"], paramNames: ["id"], handlers: { GET: engagements___id.GET, PATCH: engagements___id.PATCH } },
  { segments: ["sentiments","[id]"], paramNames: ["id"], handlers: { GET: sentiments___id.GET, PATCH: sentiments___id.PATCH, DELETE: sentiments___id.DELETE } },
  { segments: ["[id]"], paramNames: ["id"], handlers: { GET: _id.GET, PATCH: _id.PATCH, DELETE: _id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path?: string[] }> },
) {
  const { path = [] } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
