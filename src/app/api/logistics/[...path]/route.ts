/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/logistics/*
 * Consolidates 15 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as carriers from '@/lib/api-handlers/logistics/carriers';
import * as freight from '@/lib/api-handlers/logistics/freight';
import * as metrics from '@/lib/api-handlers/logistics/metrics';
import * as routes from '@/lib/api-handlers/logistics/routes';
import * as shipments from '@/lib/api-handlers/logistics/shipments';
import * as stats from '@/lib/api-handlers/logistics/stats';
import * as freight___id__load from '@/lib/api-handlers/logistics/freight___id__load';
import * as freight___id__unload from '@/lib/api-handlers/logistics/freight___id__unload';
import * as shipments___id__book from '@/lib/api-handlers/logistics/shipments___id__book';
import * as shipments___id__deliver from '@/lib/api-handlers/logistics/shipments___id__deliver';
import * as shipments___id__pickup from '@/lib/api-handlers/logistics/shipments___id__pickup';
import * as carriers___id from '@/lib/api-handlers/logistics/carriers___id';
import * as freight___id from '@/lib/api-handlers/logistics/freight___id';
import * as routes___id from '@/lib/api-handlers/logistics/routes___id';
import * as shipments___id from '@/lib/api-handlers/logistics/shipments___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["carriers"], paramNames: [], handlers: { GET: carriers.GET, POST: carriers.POST } },
  { segments: ["freight"], paramNames: [], handlers: { GET: freight.GET, POST: freight.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["routes"], paramNames: [], handlers: { GET: routes.GET, POST: routes.POST } },
  { segments: ["shipments"], paramNames: [], handlers: { GET: shipments.GET, POST: shipments.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["freight","[id]","load"], paramNames: ["id"], handlers: { POST: freight___id__load.POST } },
  { segments: ["freight","[id]","unload"], paramNames: ["id"], handlers: { POST: freight___id__unload.POST } },
  { segments: ["shipments","[id]","book"], paramNames: ["id"], handlers: { POST: shipments___id__book.POST } },
  { segments: ["shipments","[id]","deliver"], paramNames: ["id"], handlers: { POST: shipments___id__deliver.POST } },
  { segments: ["shipments","[id]","pickup"], paramNames: ["id"], handlers: { POST: shipments___id__pickup.POST } },
  { segments: ["carriers","[id]"], paramNames: ["id"], handlers: { GET: carriers___id.GET, PATCH: carriers___id.PATCH, DELETE: carriers___id.DELETE } },
  { segments: ["freight","[id]"], paramNames: ["id"], handlers: { GET: freight___id.GET, PATCH: freight___id.PATCH, DELETE: freight___id.DELETE } },
  { segments: ["routes","[id]"], paramNames: ["id"], handlers: { GET: routes___id.GET, PATCH: routes___id.PATCH, DELETE: routes___id.DELETE } },
  { segments: ["shipments","[id]"], paramNames: ["id"], handlers: { GET: shipments___id.GET, PATCH: shipments___id.PATCH, DELETE: shipments___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
