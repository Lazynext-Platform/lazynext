/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/analytics/*
 * Consolidates 20 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as metrics__series from '@/lib/api-handlers/analytics/metrics__series';
import * as agent_performance from '@/lib/api-handlers/analytics/agent_performance';
import * as dashboard from '@/lib/api-handlers/analytics/dashboard';
import * as dashboards from '@/lib/api-handlers/analytics/dashboards';
import * as forecast from '@/lib/api-handlers/analytics/forecast';
import * as ga4 from '@/lib/api-handlers/analytics/ga4';
import * as goals from '@/lib/api-handlers/analytics/goals';
import * as hub from '@/lib/api-handlers/analytics/hub';
import * as kpis from '@/lib/api-handlers/analytics/kpis';
import * as metrics from '@/lib/api-handlers/analytics/metrics';
import * as query from '@/lib/api-handlers/analytics/query';
import * as reports from '@/lib/api-handlers/analytics/reports';
import * as stats from '@/lib/api-handlers/analytics/stats';
import * as system_health from '@/lib/api-handlers/analytics/system_health';
import * as trends from '@/lib/api-handlers/analytics/trends';
import * as dashboards___id__export from '@/lib/api-handlers/analytics/dashboards___id__export';
import * as reports___id__export from '@/lib/api-handlers/analytics/reports___id__export';
import * as reports___id__run from '@/lib/api-handlers/analytics/reports___id__run';
import * as dashboards___id from '@/lib/api-handlers/analytics/dashboards___id';
import * as reports___id from '@/lib/api-handlers/analytics/reports___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routes: RouteEntry[] = [
  { segments: ["metrics","series"], paramNames: [], handlers: { GET: metrics__series.GET } },
  { segments: ["agent-performance"], paramNames: [], handlers: { GET: agent_performance.GET } },
  { segments: ["dashboard"], paramNames: [], handlers: { GET: dashboard.GET } },
  { segments: ["dashboards"], paramNames: [], handlers: { GET: dashboards.GET, POST: dashboards.POST } },
  { segments: ["forecast"], paramNames: [], handlers: { GET: forecast.GET } },
  { segments: ["ga4"], paramNames: [], handlers: { POST: ga4.POST } },
  { segments: ["goals"], paramNames: [], handlers: { GET: goals.GET } },
  { segments: ["hub"], paramNames: [], handlers: { GET: hub.GET } },
  { segments: ["kpis"], paramNames: [], handlers: { GET: kpis.GET } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET, POST: metrics.POST } },
  { segments: ["query"], paramNames: [], handlers: { POST: query.POST } },
  { segments: ["reports"], paramNames: [], handlers: { GET: reports.GET, POST: reports.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["system-health"], paramNames: [], handlers: { GET: system_health.GET } },
  { segments: ["trends"], paramNames: [], handlers: { GET: trends.GET } },
  { segments: ["dashboards","[id]","export"], paramNames: ["id"], handlers: { GET: dashboards___id__export.GET } },
  { segments: ["reports","[id]","export"], paramNames: ["id"], handlers: { GET: reports___id__export.GET } },
  { segments: ["reports","[id]","run"], paramNames: ["id"], handlers: { POST: reports___id__run.POST } },
  { segments: ["dashboards","[id]"], paramNames: ["id"], handlers: { GET: dashboards___id.GET, PATCH: dashboards___id.PATCH, DELETE: dashboards___id.DELETE } },
  { segments: ["reports","[id]"], paramNames: ["id"], handlers: { GET: reports___id.GET, PATCH: reports___id.PATCH, DELETE: reports___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
