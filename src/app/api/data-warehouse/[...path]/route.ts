/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/data-warehouse/*
 * Consolidates 17 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as jobs from '@/lib/api-handlers/data-warehouse/jobs';
import * as metrics from '@/lib/api-handlers/data-warehouse/metrics';
import * as models from '@/lib/api-handlers/data-warehouse/models';
import * as pipelines from '@/lib/api-handlers/data-warehouse/pipelines';
import * as quality from '@/lib/api-handlers/data-warehouse/quality';
import * as stats from '@/lib/api-handlers/data-warehouse/stats';
import * as jobs___id__complete from '@/lib/api-handlers/data-warehouse/jobs___id__complete';
import * as jobs___id__fail from '@/lib/api-handlers/data-warehouse/jobs___id__fail';
import * as jobs___id__run from '@/lib/api-handlers/data-warehouse/jobs___id__run';
import * as models___id__deprecate from '@/lib/api-handlers/data-warehouse/models___id__deprecate';
import * as models___id__publish from '@/lib/api-handlers/data-warehouse/models___id__publish';
import * as pipelines___id__activate from '@/lib/api-handlers/data-warehouse/pipelines___id__activate';
import * as pipelines___id__pause from '@/lib/api-handlers/data-warehouse/pipelines___id__pause';
import * as jobs___id from '@/lib/api-handlers/data-warehouse/jobs___id';
import * as models___id from '@/lib/api-handlers/data-warehouse/models___id';
import * as pipelines___id from '@/lib/api-handlers/data-warehouse/pipelines___id';
import * as quality___id from '@/lib/api-handlers/data-warehouse/quality___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["jobs"], paramNames: [], handlers: { GET: jobs.GET, POST: jobs.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["models"], paramNames: [], handlers: { GET: models.GET, POST: models.POST } },
  { segments: ["pipelines"], paramNames: [], handlers: { GET: pipelines.GET, POST: pipelines.POST } },
  { segments: ["quality"], paramNames: [], handlers: { GET: quality.GET, POST: quality.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["jobs","[id]","complete"], paramNames: ["id"], handlers: { POST: jobs___id__complete.POST } },
  { segments: ["jobs","[id]","fail"], paramNames: ["id"], handlers: { POST: jobs___id__fail.POST } },
  { segments: ["jobs","[id]","run"], paramNames: ["id"], handlers: { POST: jobs___id__run.POST } },
  { segments: ["models","[id]","deprecate"], paramNames: ["id"], handlers: { POST: models___id__deprecate.POST } },
  { segments: ["models","[id]","publish"], paramNames: ["id"], handlers: { POST: models___id__publish.POST } },
  { segments: ["pipelines","[id]","activate"], paramNames: ["id"], handlers: { POST: pipelines___id__activate.POST } },
  { segments: ["pipelines","[id]","pause"], paramNames: ["id"], handlers: { POST: pipelines___id__pause.POST } },
  { segments: ["jobs","[id]"], paramNames: ["id"], handlers: { GET: jobs___id.GET, PATCH: jobs___id.PATCH, DELETE: jobs___id.DELETE } },
  { segments: ["models","[id]"], paramNames: ["id"], handlers: { GET: models___id.GET, PATCH: models___id.PATCH, DELETE: models___id.DELETE } },
  { segments: ["pipelines","[id]"], paramNames: ["id"], handlers: { GET: pipelines___id.GET, PATCH: pipelines___id.PATCH, DELETE: pipelines___id.DELETE } },
  { segments: ["quality","[id]"], paramNames: ["id"], handlers: { GET: quality___id.GET, PATCH: quality___id.PATCH, DELETE: quality___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
