/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/franchise/*
 * Consolidates 20 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as agreements from '@/lib/api-handlers/franchise/agreements';
import * as compliance from '@/lib/api-handlers/franchise/compliance';
import * as franchisees from '@/lib/api-handlers/franchise/franchisees';
import * as metrics from '@/lib/api-handlers/franchise/metrics';
import * as royalties from '@/lib/api-handlers/franchise/royalties';
import * as stats from '@/lib/api-handlers/franchise/stats';
import * as territories from '@/lib/api-handlers/franchise/territories';
import * as agreements___id__renew from '@/lib/api-handlers/franchise/agreements___id__renew';
import * as agreements___id__sign from '@/lib/api-handlers/franchise/agreements___id__sign';
import * as agreements___id__terminate from '@/lib/api-handlers/franchise/agreements___id__terminate';
import * as compliance___id__resolve from '@/lib/api-handlers/franchise/compliance___id__resolve';
import * as franchisees___id__activate from '@/lib/api-handlers/franchise/franchisees___id__activate';
import * as franchisees___id__terminate from '@/lib/api-handlers/franchise/franchisees___id__terminate';
import * as royalties___id__payment from '@/lib/api-handlers/franchise/royalties___id__payment';
import * as territories___id__assign from '@/lib/api-handlers/franchise/territories___id__assign';
import * as agreements___id from '@/lib/api-handlers/franchise/agreements___id';
import * as compliance___id from '@/lib/api-handlers/franchise/compliance___id';
import * as franchisees___id from '@/lib/api-handlers/franchise/franchisees___id';
import * as royalties___id from '@/lib/api-handlers/franchise/royalties___id';
import * as territories___id from '@/lib/api-handlers/franchise/territories___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routes: RouteEntry[] = [
  { segments: ["agreements"], paramNames: [], handlers: { GET: agreements.GET, POST: agreements.POST } },
  { segments: ["compliance"], paramNames: [], handlers: { GET: compliance.GET, POST: compliance.POST } },
  { segments: ["franchisees"], paramNames: [], handlers: { GET: franchisees.GET, POST: franchisees.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["royalties"], paramNames: [], handlers: { GET: royalties.GET, POST: royalties.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["territories"], paramNames: [], handlers: { GET: territories.GET, POST: territories.POST } },
  { segments: ["agreements","[id]","renew"], paramNames: ["id"], handlers: { POST: agreements___id__renew.POST } },
  { segments: ["agreements","[id]","sign"], paramNames: ["id"], handlers: { POST: agreements___id__sign.POST } },
  { segments: ["agreements","[id]","terminate"], paramNames: ["id"], handlers: { POST: agreements___id__terminate.POST } },
  { segments: ["compliance","[id]","resolve"], paramNames: ["id"], handlers: { POST: compliance___id__resolve.POST } },
  { segments: ["franchisees","[id]","activate"], paramNames: ["id"], handlers: { POST: franchisees___id__activate.POST } },
  { segments: ["franchisees","[id]","terminate"], paramNames: ["id"], handlers: { POST: franchisees___id__terminate.POST } },
  { segments: ["royalties","[id]","payment"], paramNames: ["id"], handlers: { POST: royalties___id__payment.POST } },
  { segments: ["territories","[id]","assign"], paramNames: ["id"], handlers: { POST: territories___id__assign.POST } },
  { segments: ["agreements","[id]"], paramNames: ["id"], handlers: { GET: agreements___id.GET, PATCH: agreements___id.PATCH } },
  { segments: ["compliance","[id]"], paramNames: ["id"], handlers: { GET: compliance___id.GET, PATCH: compliance___id.PATCH } },
  { segments: ["franchisees","[id]"], paramNames: ["id"], handlers: { GET: franchisees___id.GET, PATCH: franchisees___id.PATCH } },
  { segments: ["royalties","[id]"], paramNames: ["id"], handlers: { GET: royalties___id.GET, PATCH: royalties___id.PATCH } },
  { segments: ["territories","[id]"], paramNames: ["id"], handlers: { GET: territories___id.GET, PATCH: territories___id.PATCH, DELETE: territories___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
