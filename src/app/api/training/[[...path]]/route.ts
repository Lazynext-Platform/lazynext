/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/training/*
 * Consolidates 18 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as certifications__expiring from '@/lib/api-handlers/training/certifications__expiring';
import * as assessments from '@/lib/api-handlers/training/assessments';
import * as certifications from '@/lib/api-handlers/training/certifications';
import * as courses from '@/lib/api-handlers/training/courses';
import * as enrollments from '@/lib/api-handlers/training/enrollments';
import * as learning_paths from '@/lib/api-handlers/training/learning_paths';
import * as metrics from '@/lib/api-handlers/training/metrics';
import * as stats from '@/lib/api-handlers/training/stats';
import * as assessments___id__submit from '@/lib/api-handlers/training/assessments___id__submit';
import * as certifications___id__revoke from '@/lib/api-handlers/training/certifications___id__revoke';
import * as certifications___id__verify from '@/lib/api-handlers/training/certifications___id__verify';
import * as enrollments___id__complete from '@/lib/api-handlers/training/enrollments___id__complete';
import * as enrollments___id__drop from '@/lib/api-handlers/training/enrollments___id__drop';
import * as assessments___id from '@/lib/api-handlers/training/assessments___id';
import * as certifications___id from '@/lib/api-handlers/training/certifications___id';
import * as courses___id from '@/lib/api-handlers/training/courses___id';
import * as enrollments___id from '@/lib/api-handlers/training/enrollments___id';
import * as learning_paths___id from '@/lib/api-handlers/training/learning_paths___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["certifications","expiring"], paramNames: [], handlers: { GET: certifications__expiring.GET } },
  { segments: ["assessments"], paramNames: [], handlers: { GET: assessments.GET, POST: assessments.POST } },
  { segments: ["certifications"], paramNames: [], handlers: { GET: certifications.GET, POST: certifications.POST } },
  { segments: ["courses"], paramNames: [], handlers: { GET: courses.GET, POST: courses.POST } },
  { segments: ["enrollments"], paramNames: [], handlers: { GET: enrollments.GET, POST: enrollments.POST } },
  { segments: ["learning-paths"], paramNames: [], handlers: { GET: learning_paths.GET, POST: learning_paths.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["assessments","[id]","submit"], paramNames: ["id"], handlers: { POST: assessments___id__submit.POST } },
  { segments: ["certifications","[id]","revoke"], paramNames: ["id"], handlers: { POST: certifications___id__revoke.POST } },
  { segments: ["certifications","[id]","verify"], paramNames: ["id"], handlers: { POST: certifications___id__verify.POST } },
  { segments: ["enrollments","[id]","complete"], paramNames: ["id"], handlers: { POST: enrollments___id__complete.POST } },
  { segments: ["enrollments","[id]","drop"], paramNames: ["id"], handlers: { POST: enrollments___id__drop.POST } },
  { segments: ["assessments","[id]"], paramNames: ["id"], handlers: { GET: assessments___id.GET, PATCH: assessments___id.PATCH, DELETE: assessments___id.DELETE } },
  { segments: ["certifications","[id]"], paramNames: ["id"], handlers: { GET: certifications___id.GET, PATCH: certifications___id.PATCH } },
  { segments: ["courses","[id]"], paramNames: ["id"], handlers: { GET: courses___id.GET, PATCH: courses___id.PATCH, DELETE: courses___id.DELETE } },
  { segments: ["enrollments","[id]"], paramNames: ["id"], handlers: { GET: enrollments___id.GET, PATCH: enrollments___id.PATCH } },
  { segments: ["learning-paths","[id]"], paramNames: ["id"], handlers: { GET: learning_paths___id.GET, PATCH: learning_paths___id.PATCH, DELETE: learning_paths___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path?: string[] }> },
) {
  const { path = [] } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
