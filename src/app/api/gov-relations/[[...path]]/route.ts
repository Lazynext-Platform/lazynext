/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/gov-relations/*
 * Consolidates 16 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as compliance from '@/lib/api-handlers/gov-relations/compliance';
import * as contacts from '@/lib/api-handlers/gov-relations/contacts';
import * as lobbying from '@/lib/api-handlers/gov-relations/lobbying';
import * as metrics from '@/lib/api-handlers/gov-relations/metrics';
import * as policies from '@/lib/api-handlers/gov-relations/policies';
import * as stats from '@/lib/api-handlers/gov-relations/stats';
import * as compliance___id__approve from '@/lib/api-handlers/gov-relations/compliance___id__approve';
import * as compliance___id__file from '@/lib/api-handlers/gov-relations/compliance___id__file';
import * as lobbying___id__cancel from '@/lib/api-handlers/gov-relations/lobbying___id__cancel';
import * as lobbying___id__complete from '@/lib/api-handlers/gov-relations/lobbying___id__complete';
import * as lobbying___id__register from '@/lib/api-handlers/gov-relations/lobbying___id__register';
import * as policies___id__position from '@/lib/api-handlers/gov-relations/policies___id__position';
import * as compliance___id from '@/lib/api-handlers/gov-relations/compliance___id';
import * as contacts___id from '@/lib/api-handlers/gov-relations/contacts___id';
import * as lobbying___id from '@/lib/api-handlers/gov-relations/lobbying___id';
import * as policies___id from '@/lib/api-handlers/gov-relations/policies___id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["compliance"], paramNames: [], handlers: { GET: compliance.GET, POST: compliance.POST } },
  { segments: ["contacts"], paramNames: [], handlers: { GET: contacts.GET, POST: contacts.POST } },
  { segments: ["lobbying"], paramNames: [], handlers: { GET: lobbying.GET, POST: lobbying.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { GET: metrics.GET } },
  { segments: ["policies"], paramNames: [], handlers: { GET: policies.GET, POST: policies.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: ["compliance","[id]","approve"], paramNames: ["id"], handlers: { POST: compliance___id__approve.POST } },
  { segments: ["compliance","[id]","file"], paramNames: ["id"], handlers: { POST: compliance___id__file.POST } },
  { segments: ["lobbying","[id]","cancel"], paramNames: ["id"], handlers: { POST: lobbying___id__cancel.POST } },
  { segments: ["lobbying","[id]","complete"], paramNames: ["id"], handlers: { POST: lobbying___id__complete.POST } },
  { segments: ["lobbying","[id]","register"], paramNames: ["id"], handlers: { POST: lobbying___id__register.POST } },
  { segments: ["policies","[id]","position"], paramNames: ["id"], handlers: { POST: policies___id__position.POST } },
  { segments: ["compliance","[id]"], paramNames: ["id"], handlers: { GET: compliance___id.GET, PATCH: compliance___id.PATCH } },
  { segments: ["contacts","[id]"], paramNames: ["id"], handlers: { GET: contacts___id.GET, PATCH: contacts___id.PATCH, DELETE: contacts___id.DELETE } },
  { segments: ["lobbying","[id]"], paramNames: ["id"], handlers: { GET: lobbying___id.GET, PATCH: lobbying___id.PATCH } },
  { segments: ["policies","[id]"], paramNames: ["id"], handlers: { GET: policies___id.GET, PATCH: policies___id.PATCH, DELETE: policies___id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path?: string[] }> },
) {
  const { path = [] } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path?: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
