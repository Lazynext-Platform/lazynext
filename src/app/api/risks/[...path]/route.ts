/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/risks/*
 * Consolidates 16 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as mitigations__overdue from '@/lib/api-handlers/risks/mitigations__overdue';
import * as mitigations__stats from '@/lib/api-handlers/risks/mitigations__stats';
import * as by_category from '@/lib/api-handlers/risks/by_category';
import * as by_level from '@/lib/api-handlers/risks/by_level';
import * as high from '@/lib/api-handlers/risks/high';
import * as matrix from '@/lib/api-handlers/risks/matrix';
import * as mitigations from '@/lib/api-handlers/risks/mitigations';
import * as stats from '@/lib/api-handlers/risks/stats';
import * as index from '@/lib/api-handlers/risks/index';
import * as mitigations___id__status from '@/lib/api-handlers/risks/mitigations___id__status';
import * as _id__assess from '@/lib/api-handlers/risks/_id__assess';
import * as _id__mitigation from '@/lib/api-handlers/risks/_id__mitigation';
import * as _id__owner from '@/lib/api-handlers/risks/_id__owner';
import * as _id__status from '@/lib/api-handlers/risks/_id__status';
import * as mitigations___id from '@/lib/api-handlers/risks/mitigations___id';
import * as _id from '@/lib/api-handlers/risks/_id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, any>;
}

const routeTable: RouteEntry[] = [
  { segments: ["mitigations","overdue"], paramNames: [], handlers: { GET: mitigations__overdue.GET } },
  { segments: ["mitigations","stats"], paramNames: [], handlers: { GET: mitigations__stats.GET } },
  { segments: ["by-category"], paramNames: [], handlers: { GET: by_category.GET } },
  { segments: ["by-level"], paramNames: [], handlers: { GET: by_level.GET } },
  { segments: ["high"], paramNames: [], handlers: { GET: high.GET } },
  { segments: ["matrix"], paramNames: [], handlers: { GET: matrix.GET } },
  { segments: ["mitigations"], paramNames: [], handlers: { GET: mitigations.GET, POST: mitigations.POST } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: [], paramNames: [], handlers: { GET: index.GET, POST: index.POST } },
  { segments: ["mitigations","[id]","status"], paramNames: ["id"], handlers: { POST: mitigations___id__status.POST } },
  { segments: ["[id]","assess"], paramNames: ["id"], handlers: { POST: _id__assess.POST } },
  { segments: ["[id]","mitigation"], paramNames: ["id"], handlers: { POST: _id__mitigation.POST } },
  { segments: ["[id]","owner"], paramNames: ["id"], handlers: { POST: _id__owner.POST } },
  { segments: ["[id]","status"], paramNames: ["id"], handlers: { POST: _id__status.POST } },
  { segments: ["mitigations","[id]"], paramNames: ["id"], handlers: { GET: mitigations___id.GET, PATCH: mitigations___id.PATCH, DELETE: mitigations___id.DELETE } },
  { segments: ["[id]"], paramNames: ["id"], handlers: { GET: _id.GET, PATCH: _id.PATCH, DELETE: _id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routeTable) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatchRequest(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatchRequest(req, 'OPTIONS', ctx);
}
