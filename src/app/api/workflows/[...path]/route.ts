/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/workflows/*
 * Consolidates 15 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as node_library from '@/lib/api-handlers/workflows/node_library';
import * as stats from '@/lib/api-handlers/workflows/stats';
import * as index from '@/lib/api-handlers/workflows/index';
import * as executions___id__cancel from '@/lib/api-handlers/workflows/executions___id__cancel';
import * as executions___id__resume from '@/lib/api-handlers/workflows/executions___id__resume';
import * as _id__duplicate from '@/lib/api-handlers/workflows/_id__duplicate';
import * as _id__execute from '@/lib/api-handlers/workflows/_id__execute';
import * as _id__executions from '@/lib/api-handlers/workflows/_id__executions';
import * as _id__publish from '@/lib/api-handlers/workflows/_id__publish';
import * as _id__test_run from '@/lib/api-handlers/workflows/_id__test_run';
import * as _id__unpublish from '@/lib/api-handlers/workflows/_id__unpublish';
import * as _id__validate from '@/lib/api-handlers/workflows/_id__validate';
import * as _id__versions from '@/lib/api-handlers/workflows/_id__versions';
import * as executions___id from '@/lib/api-handlers/workflows/executions___id';
import * as _id from '@/lib/api-handlers/workflows/_id';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["node-library"], paramNames: [], handlers: { GET: node_library.GET } },
  { segments: ["stats"], paramNames: [], handlers: { GET: stats.GET } },
  { segments: [], paramNames: [], handlers: { GET: index.GET, POST: index.POST } },
  { segments: ["executions","[id]","cancel"], paramNames: ["id"], handlers: { POST: executions___id__cancel.POST } },
  { segments: ["executions","[id]","resume"], paramNames: ["id"], handlers: { POST: executions___id__resume.POST } },
  { segments: ["[id]","duplicate"], paramNames: ["id"], handlers: { POST: _id__duplicate.POST } },
  { segments: ["[id]","execute"], paramNames: ["id"], handlers: { POST: _id__execute.POST } },
  { segments: ["[id]","executions"], paramNames: ["id"], handlers: { GET: _id__executions.GET } },
  { segments: ["[id]","publish"], paramNames: ["id"], handlers: { POST: _id__publish.POST } },
  { segments: ["[id]","test-run"], paramNames: ["id"], handlers: { POST: _id__test_run.POST } },
  { segments: ["[id]","unpublish"], paramNames: ["id"], handlers: { POST: _id__unpublish.POST } },
  { segments: ["[id]","validate"], paramNames: ["id"], handlers: { POST: _id__validate.POST } },
  { segments: ["[id]","versions"], paramNames: ["id"], handlers: { GET: _id__versions.GET } },
  { segments: ["executions","[id]"], paramNames: ["id"], handlers: { GET: executions___id.GET } },
  { segments: ["[id]"], paramNames: ["id"], handlers: { GET: _id.GET, PATCH: _id.PATCH, DELETE: _id.DELETE } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
