/**
 * AUTO-GENERATED by scripts/consolidate-all-routes.mjs
 * Catch-all route handler for /api/ads/*
 * Consolidates 11 routes into a single entry point.
 */
import { NextRequest, NextResponse } from 'next/server';
import * as budget from '@/lib/api-handlers/ads/budget';
import * as create from '@/lib/api-handlers/ads/create';
import * as google_approve from '@/lib/api-handlers/ads/google_approve';
import * as google_budget from '@/lib/api-handlers/ads/google_budget';
import * as google_report from '@/lib/api-handlers/ads/google_report';
import * as google_safety from '@/lib/api-handlers/ads/google_safety';
import * as list from '@/lib/api-handlers/ads/list';
import * as meta_approve from '@/lib/api-handlers/ads/meta_approve';
import * as meta_safety from '@/lib/api-handlers/ads/meta_safety';
import * as metrics from '@/lib/api-handlers/ads/metrics';
import * as report from '@/lib/api-handlers/ads/report';

interface RouteEntry {
  segments: string[];
  paramNames: string[];
  // eslint-disable-next-line @typescript-eslint/no-explicit-any
  handlers: Record<string, (req: NextRequest, ctx?: { params: Record<string, string> }) => any>;
}

const routes: RouteEntry[] = [
  { segments: ["budget"], paramNames: [], handlers: { PATCH: budget.PATCH } },
  { segments: ["create"], paramNames: [], handlers: { POST: create.POST } },
  { segments: ["google-approve"], paramNames: [], handlers: { GET: google_approve.GET, POST: google_approve.POST } },
  { segments: ["google-budget"], paramNames: [], handlers: { PATCH: google_budget.PATCH } },
  { segments: ["google-report"], paramNames: [], handlers: { GET: google_report.GET } },
  { segments: ["google-safety"], paramNames: [], handlers: { GET: google_safety.GET, POST: google_safety.POST } },
  { segments: ["list"], paramNames: [], handlers: { GET: list.GET } },
  { segments: ["meta-approve"], paramNames: [], handlers: { GET: meta_approve.GET, POST: meta_approve.POST } },
  { segments: ["meta-safety"], paramNames: [], handlers: { GET: meta_safety.GET, POST: meta_safety.POST } },
  { segments: ["metrics"], paramNames: [], handlers: { POST: metrics.POST } },
  { segments: ["report"], paramNames: [], handlers: { GET: report.GET } },
];

function matchRoute(pathSegments: string[]): { route: RouteEntry; params: Record<string, string> } | null {
  for (const route of routes) {
    if (route.segments.length !== pathSegments.length) continue;
    const params: Record<string, string> = {};
    let matched = true;
    for (let i = 0; i < route.segments.length; i++) {
      const pattern = route.segments[i];
      const actual = pathSegments[i];
      if (pattern.startsWith('[') && pattern.endsWith(']')) {
        // Dynamic segment - extract param
        const paramName = pattern.slice(1, -1).replace(/\./g, '_');
        params[paramName] = actual;
      } else if (pattern !== actual) {
        matched = false;
        break;
      }
    }
    if (matched) return { route, params };
  }
  return null;
}

function extractParams(pattern: string, actual: string): string {
  return actual;
}

async function dispatch(
  req: NextRequest,
  method: string,
  { params }: { params: Promise<{ path: string[] }> },
) {
  const { path } = await params;
  const match = matchRoute(path);
  if (!match) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 });
  }
  const handler = match.route.handlers[method];
  if (!handler) {
    return NextResponse.json({ error: 'method_not_allowed' }, { status: 405 });
  }
  const ctx = match.route.paramNames.length > 0 ? { params: match.params } : undefined;
  return handler(req, ctx);
}

export async function GET(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'GET', ctx);
}

export async function POST(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'POST', ctx);
}

export async function PUT(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PUT', ctx);
}

export async function PATCH(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'PATCH', ctx);
}

export async function DELETE(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'DELETE', ctx);
}

export async function HEAD(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'HEAD', ctx);
}

export async function OPTIONS(req: NextRequest, ctx: { params: Promise<{ path: string[] }> }) {
  return dispatch(req, 'OPTIONS', ctx);
}
