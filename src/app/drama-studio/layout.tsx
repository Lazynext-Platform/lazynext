import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'AI Drama Ad Studio — Lazynext',
};

export default function DramaStudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
